# 🗺️ 개발 로드맵

스팀 출시를 목표로 천천히, 단계별로 완성해 나가는 계획.
각 마일스톤은 "플레이 가능한 상태"를 유지하면서 쌓아 올린다.

## ✅ M0. 웹 프로토타입 (완료)

- HTML5 Canvas로 핵심 루프 검증: 경작 → 파종 → 물주기 → 수확 → 판매 → 취침
- Godot으로 옮긴 뒤 역할이 끝나, 프로토타입 폴더는 M51에서 삭제했다

## ✅ M1. Godot 이전 — 핵심 루프 (완료)

- Godot 4.4 프로젝트 셋업 (`game/`), 픽셀 퍼펙트 480x320 뷰포트
- 웹 버전과 동일한 핵심 루프 이식
- 작물 4종, 시간/기력/낮밤, 상점, 저장/불러오기 (`user://` JSON)
- 한글 도트 폰트(Unifont 서브셋) 적용

## ✅ M2. 게임 구조 다듬기 (완료)

- [x] 입력을 InputMap 액션으로 이전 (project.godot 정의, 키 리바인딩 대비)
- [x] 씬 분리: player.tscn / hud.tscn / shop.tscn / summary.tscn
- [x] TileMapLayer 도입 검토 → **보류 결정**: 경작 상태(밭/물/작물)가 전부
      런타임 데이터라 현재의 커스텀 드로잉이 더 단순함. 맵을 에디터에서
      손으로 꾸미기 시작하는 M4(넓은 맵) 시점에 재검토.
- [x] 마우스 클릭 조작 지원 (인접 타일 클릭 = 그 방향으로 도구 사용)
- [x] 일별 결산 화면 (수확량, 판매 수입, 씨앗 지출, 소지금)

## ✅ M3. 계절과 날씨 (완료)

- [x] 봄/여름/가을/겨울 (계절당 28일), 계절별 잔디/나무 색 (겨울은 설경)
- [x] 작물 10종으로 확장: 봄(감자·당근·딸기) / 여름(토마토·옥수수·수박) /
      가을(호박·가지·배추, 옥수수는 여름~가을) / 겨울(겨울무)
- [x] 상점은 제철 씨앗만 판매, 제철 아닌 씨앗은 심을 수 없음
- [x] 날씨: 비 오는 날 밭 자동 물주기 + 빗줄기/어둑한 화면, 겨울엔 눈 연출
      (날씨는 날짜 해시로 결정적 — 저장 불필요) / 빗소리는 M7 사운드에서
- [x] 계절이 바뀌면 제철 아닌 작물은 시듦 → 호미로 정리
- [x] HUD에 계절/날짜/날씨 표시, 아침 결산에 날씨 예보 포함

## ✅ M4. 콘텐츠 확장 (완료)

- [x] 더 넓은 맵 (60x40, 기존의 4배) + 카메라 스크롤 (Camera2D 스무딩)
- [x] 나무 베기(도끼, 3타) / 돌 캐기(곡괭이, 2타) → 목재/석재 자원
      - 매일 아침 빈 땅에 나무/돌이 조금씩 다시 생긴다
- [x] 설치물: 울타리(목재 1), 스프링클러(목재 2+석재 2, 아침마다 주변 4칸 물주기)
      - 도끼/곡괭이로 회수 가능 (재료 반환)
- [x] 도구 업그레이드: 호미/물뿌리개 Lv2 (전방 3칸, 상점 업그레이드 탭)
- [x] 저장 확장: 오브젝트(나무/돌/설치물)와 자원/도구 레벨 저장
- [ ] 거대작물/품질 등급 → M7 폴리싱 단계로 이동 (작물 추가는 M3에서 완료)

## ✅ M5. 동물과 낚시 (완료)

- [x] 동물: 상점에서 닭(800G)/소(1500G) 입양, 농장을 자유롭게 배회
      - E로 쓰다듬으면 다음 날 아침 달걀(60G)/우유(120G) 생산 (최대 8마리)
      - 축사/사료 아이템은 M7 폴리싱에서 검토
- [x] 낚시: 낚싯대(9)로 물가 캐스팅 → 입질(!) 타이밍 → 타이밍 바 미니게임
      - 물고기 4종 (붕어/잉어/메기/황금잉어), 희귀할수록 좁은 판정 구간
- [x] 물고기 도감: 상점 도감 탭, 낚아본 물고기만 공개 + 누적 마릿수
- [x] 판매 확장: 동물 생산물/물고기도 판매 탭에서 판매

## ✅ M6. NPC와 마을 (완료)

- [x] 마을 구역 (맵 동쪽): 상점 오두막(E로 상점 열기), 철수네 집, 의뢰 게시판
- [x] NPC 2명 (상인 민지, 낚시꾼 철수): 마을을 배회, E로 대화
- [x] 호감도: 매일 첫 대화 +2, 선물 +10, 대화창에 ♥ 표시
      - 민지 호감도 50 이상이면 씨앗 10% 할인
- [x] 의뢰 게시판: 매일 제철 작물 납품 의뢰 (보상 1.5배), 수락/납품
- [x] UI 개편: 텍스트 도구 목록 → 도트 아이콘 핫바 (클릭 선택 가능)
- [x] 상황 힌트: 타겟 타일 위에 "수확!/성장중 n/m일/물주기!/E: 대화" 등 표시
- [ ] 마을 별도 맵 분리는 규모가 커지면 재검토

## ✅ M7. 연출과 사운드 (완료)

- [x] BGM 4곡: 같은 테마를 계절별로 편곡 (조/템포/파형 변화, 겨울은 성긴 사인파)
      - 외부 에셋 없이 파이썬 신디사이저 스크립트로 WAV 생성 (칩튠)
- [x] 효과음 17종: 도구별/낚시/동물/UI/동전/발소리(2종 교차) 등
- [x] 오디오 버스(Master/BGM/SFX) + 사운드 매니저 오토로드
- [x] 픽셀 파티클: 물방울/수확 반짝임/나무·돌 파편/흙/씨앗
- [x] 화면 전환: 취침 페이드, 씬 진입 페이드 인
- [x] 타이틀 화면: 이어하기/새로 시작/설정/종료 + 도트 장식
- [x] 설정: 마스터/BGM/효과음 볼륨, 전체화면 (user://에 저장)
- [x] ESC → 게임 메뉴 (저장 후 타이틀로)
- [x] 튜토리얼: 새 게임 시 환영 대화 + "다음 목표" 안내
      (이동→밭갈기→파종→물주기→취침→수확→벌목→채광→설치→낚시→상점,
      체크리스트 방식이라 순서 자유, 건너뛰기 가능, 진행 상태 저장)
- [x] 도구 해금: 새 게임은 호미만 들고 시작, 목표 달성마다 도구가 하나씩 열림
      (잠긴 슬롯은 빈 칸, 완료 대화창에서 기본 키 총정리)
- [x] UI 폴리싱: 핫바 슬롯에 숫자키 표시, 상단 바 배경, 발밑 그림자,
      쓰다듬은 동물 머리 위 하트
- [x] 도트 퀄리티 업: 플레이어 외곽선+음영+하이라이트, 나무 3톤 잎
      (NPC 팔레트 스왑에도 자동 반영)
- [x] NPC별 호감도 강화: 대화창에 ♥+수치(n/100) 표시, NPC별 특전
      (민지 50+: 씨앗 10% 할인 / 철수 50+: 낚시 판정 구간 25% 확대)
- [x] 오프닝 스토리: 할아버지의 편지 3페이지 연출 (어두운 화면 + 페이드 인)
- [x] 캐릭터 비율 개선: 12x16 SD → 16x24 (플레이어/NPC), 나무도 16x24로
- [x] NPC 표정 초상화: 대화창에 얼굴 표시, 선물/호감도 50+ 시 웃는 표정+홍조
- [x] 지도(M): 전체 맵 + 우리집/마을/게시판/연못 라벨 + 내 위치 깜빡임
- [x] 목표 네비게이션: 위치가 있는 목표(취침/벌목/채광/낚시/상점)는
      플레이어 주위에 방향 화살표 표시, 튜토리얼에 지도 목표 추가
- [x] 키 리바인딩 UI 완료 (M15)
- [ ] 거대작물/품질 등급 (M4에서 이월) → 폴리싱 백로그

## ✅ M9. 멀티플레이 협동 모드 - 1차 (완료)

솔로는 그대로, 스타듀밸리식 **호스트-게스트 협동** (같은 네트워크, 2~4인).

- [x] Godot 고수준 멀티플레이어(ENet): 타이틀 "함께하기" → 방 만들기 / IP 참가
- [x] 접속 시 호스트 농장 전체 스냅샷 수신 (저장 직렬화 재사용)
- [x] 플레이어 위치/방향/걷기 15Hz 동기화, 플레이어별 색조 아바타
- [x] 게스트 행동 RPC: 8가지 도구/동물 쓰다듬기/상점/선물/의뢰/낚시 성과
  - 호스트가 같은 로직을 실행해 권한 확정, 변경 타일 5x5 영역 브로드캐스트
- [x] 자원/돈/씨앗 등 공유 (스타듀식 공동 지갑), 기력은 각자
- [x] 시간/날씨/하루 진행/저장은 호스트 전담, 아침마다 전체 재동기화
- [x] 검증: 호스트+게스트 2인스턴스 자동 테스트 (게스트 경작 동기화 확인)
- [ ] 2차: 동물/NPC 위치 동기화, 게스트 취침 투표, 포트포워딩 안내,
      Steam P2P(GodotSteam 로비) 전환

## ✅ M11. 부지 시스템 (완료)

- [x] 맵 4분할: 내 농장(무료 시작) / 동쪽 들판 3,000G / 남쪽 들판 8,000G /
      숲과 호수 15,000G — 마을 구역은 공용
- [x] 미구매 부지는 이동은 자유, 사용(농사/벌목/채광/낚시)은 잠김
- [x] 부지 표지판(E)에서 구입, 인게임 어둑한 오버레이 + 금색 경계 표시
- [x] 지도(M)에 부지 이름/가격 표시, 구입 시 즉시 반영
- [x] 멀티플레이 동기화(공동 구입), 저장, 구버전 세이브는 전체 소유 호환
- [ ] 확장 아이디어: 부지별 특성(목장 부지·마을 개발 부지), 건물 건설

## ✅ M12. 실시간 성장 + 집 내부 (완료)

- [x] 작물 성장을 날짜제 → 시간제로: 젖은 밭에서 게임 시간으로 실시간 성장
      (감자 4시간 ≈ 실제 3분), 성장 게이지 % 힌트 표시
- [x] 물 지속 시간: 손 물주기 6시간, 비/스프링클러는 하루 종일 → 마르면 성장 정지
- [x] 집 입장(E) + 내부 초안: 판자 바닥/창문(시간대별 하늘)/침대/테이블/궤짝/러그
- [x] 침대 옆 E로 취침, 아래 문으로 퇴장, 집 안에서도 시간은 흐름
- [x] 구버전 세이브 성장/물 상태 자동 환산

## ✅ M10. 동굴 탐험 (완료 - 1차)

- [x] 마을 북쪽 동굴 입구(E) → 층별 랜덤 생성 동굴 (바위 벽/기둥)
- [x] 슬라임 몬스터: 가까우면 추적/멀면 배회 AI, 접촉 시 기력 -8 + 넉백
- [x] 전투: Space로 전방 공격 (스윙 이펙트, 쿨다운), 피격 시 붉게 점멸
- [x] 광석 노드 채굴 + 슬라임 드랍(광석 35%)
- [x] 층 클리어(전멸) → 보상 상자(광석+3층부터 보석) → 계단으로 다음 층
      (층마다 슬라임 수/속도/체력 증가)
- [x] 기력 바닥나면 입구로 강제 퇴장, 기력 15 미만이면 입장 불가
- [x] 새 아이템: 광석(50G)/보석(220G) — 판매/인벤토리/선물 연동, 멀티 동기화
- [x] 2차: 박쥐(2층~, 돌진 후 감속) / 유령(4층~, 벽 통과 추적, 반투명) 추가
- [x] 몬스터 도감: 상점 도감 탭에 처치 수 + 설명 (처치 전엔 ???)
- [x] 광석 강화: 호미/물뿌리개 Lv3(3x3 범위, 광석 8) /
      도끼·곡괭이 Lv2(1타 채집 + 동굴 공격력 2배, 광석 6)
- [x] 종류별 드랍: 박쥐 20% 광석, 유령 광석 확정 + 15% 보석 재료

## ✅ M13. 대형 마을 + 지역 해금 개편 (완료)

- [x] 맵 90x60으로 확장 (2.25배), 보이는 타일만 그리는 컬링 추가
- [x] 대형 마을(30x30): 잡화점/목장 상회/대장간/수산시장 + 광장(분수/게시판)
      각 건물 E로 해당 상점 탭이 열림, B는 상점 근처에서만
- [x] 지역 7개: 미해금 부지는 진입 불가, 농장->마을 공용 길 항상 개방
      신규: 강변 부지 2만(강) / 황금 평야 3만 / 깊은 숲 5만(자원+연못 밀집)
- [x] 길(path) 지형 추가, 지도에 마을/광장/도로 표시
- [x] QoL: 대상에 맞는 도구 자동 선택(나무->도끼 등), 행동 메시지 하단 표시
- [x] 개발용 시작 자금 1억G (출시 전 500G로 되돌릴 것 - game_data.gd)

## ✅ M14. 생활 콘텐츠 대확장 (완료)

- [x] 능력치(숙련도) 6종: 농사/낚시/벌목/채광/전투/요리 — 반복할수록 레벨업
      효과: 작물 성장 +4%/Lv, 입질 대기 -5%/Lv, 목재·석재·광석 추가 획득,
      동굴 공격력 +0.5/Lv, 요리 회복량 +5%/Lv (인벤토리 I에서 확인)
- [x] 집 꾸미기: 집 안에서 F로 꾸미기 모드 — 가구 8종 구입/배치/이동/판매
      (식탁·의자·궤짝·러그·화분·책장·램프·탁자, 저장/멀티 동기화)
- [x] 요리: 집 조리대(E)에서 레시피 10종 요리, 먹으면 체력 회복, 판매 가능
      상점 도감 탭에 요리 도감(??? 미발견 표시)
- [x] 펫 4종 + 고유 패시브: 강아지(이동 +10%)/고양이(입질 -15%)/
      부엉이(동굴 피해 -25%)/토끼(성장 +5%) — 목장 상회에서 입양, 따라다님
- [x] 활동력(에너지) 시스템 삭제 → 체력으로 전환: 도구/낚시 제한 없음,
      동굴 전투에서만 소모, 밖에서 자연 회복 + 요리로 즉시 회복
- [x] 오프닝 스토리 6+1페이지로 확장 (이전/다음/페이지 번호/건너뛰기)
- [x] 화면 설정: 창 960/1440/1920 + 전체 화면 (타이틀 설정, F11 토글)
- [x] 버그 수정: 안내 텍스트가 건물에 가려짐(최상위 레이어로), 물가에서
      낚싯대 자동 선택, 동굴 사다리 E로 나갈 때 동굴이 다시 열리던 문제
- [x] 부지 스토리: 부지를 구입할 때마다 장(章) 형식 스토리가 이어짐
      (제1장 동쪽 들판 → ... → 마지막 장 깊은 숲, 다음 지역을 예고하는 구성)

## ✅ M15. 조작/화면 개선 (완료)

- [x] 화면 비율 16:9 전환: 뷰포트 480x270, 창 960x540/1440x810/1920x1080
      (1080p 전체 화면 = 정수 4배 스케일로 도트가 깨끗함)
- [x] 키 설정: 타이틀 설정 → 키 설정에서 전체 키 목록 확인 + 클릭으로 리바인딩
      (충돌 시 두 액션 키 맞바꿈, user://keybinds.json에 사람마다 저장, 기본값 복원)
- [x] 퀘스트 창(J): 튜토리얼 진행 체크리스트 + 오늘의 의뢰 진행 상황 + 다음 부지
      목표를 한 창에서 확인 (튜토리얼에 J키 학습 단계 추가)
- [x] 게임 시계 1분 단위 표시 (시간이 흐르는 게 체감되게)
- [x] 버그 수정: 울타리/스프링클러를 발밑에 설치해 끼이는 문제
      (플레이어와 겹치는 타일 설치 금지 + 끼였을 때 자가 탈출)

## ✅ M16. 목표와 편의 (완료)

- [x] 최종 목표 + 엔딩: "할아버지의 농장 부활" — 6개 부지를 모두 되찾으면
      에필로그 스토리(3페이지) + 농장 부활의 기록(플레이 통계) → 이후 자유 플레이
      (퀘스트 창 J에 최종 목표 진행도 상시 표시, ending_seen 저장/동기화)
- [x] 상점별 기능 분리: 잡화점=씨앗+판매 / 목장 상회=동물·펫 /
      대장간=강화 / 수산시장=도감 — 가게마다 해당 탭만 표시
      (당시엔 B키 단축키도 있었으나 M69에서 없앴다)
- [x] 마우스 8방향 타겟: 플레이어 주변 8칸(대각선 포함)에 마우스를 올리면
      그 칸이 타겟이 됨 (하이라이트/자동 도구/클릭 사용 모두 연동)
- [x] 빈 곳에서 E를 눌러도 마을 안내 메시지가 반복되지 않게 수정

## ✅ M17. 스토리 전면 개편 — 연금술 서사 (완료)

- [x] 오프닝 재작성 (7페이지): 탐험가·연금술 연구자 할아버지, 나무 상자,
      빈 연구 노트, "내가 이루지 못한 꿈을 네가 이어주었으면" — 목표는 비공개
- [x] 연구 노트 시스템 (N): 작물/물고기/광물/몬스터/요리/주민 이야기가
      발견할 때마다 채워짐 (40페이지). 50%부터 할아버지의 숨겨진 메모 4단계 해금
- [x] 부지 스토리 재테마: 각 지역에서 할아버지의 탐사 흔적(팻말/유리병/쪽지/
      지도/실험 상자/마지막 캠프)을 발견하는 장(章) 구성
- [x] 전설 재료 7종 (콘텐츠마다 하나): 달빛 작물(농사 2%)/황금잉어(낚시)/
      세계수 가지(벌목 2%)/별빛 광석(동굴 5층+ 상자)/유령의 정수(유령 10%)/
      황금 달걀(목장 3%)/할아버지의 기억 조각(호감도 100) — 판매 불가
- [x] NPC 비밀 대사: 호감도 50/100에서 할아버지의 과거가 드러나는 대사 추가
- [x] 진 엔딩 교체: 재료 7종 + 노트 80% → 최후의 연금술 → '유니콘의 뿔' 완성
      (거대한 전투가 아닌, 한 사람의 평생의 꿈을 이어 완성하는 감동 엔딩)
      엔딩 후 자유 플레이 계속
- [ ] 백로그: 세계수 동굴(고난도 탐험 지역), 폐허/고대 유적, 계절 비밀 장소,
      NPC 추가(각자의 삶/고민/과거), 연구실 별도 공간

## ✅ M18. 가독성/화면 개편 (완료)

- [x] 폰트 교체: Unifont 16px → Galmuri9 9px 한글 픽셀 폰트 (SIL OFL)
      전체 텍스트 크기 축소 + 픽셀 퍼펙트 렌더링 (AA/서브픽셀 off)
- [x] 줌아웃: 뷰포트 480x270 → 640x360 (창 1280x720 / 1920x1080 정수 배율)
      전체 UI(패널/HUD/타이틀/집/동굴/지도) 재배치
- [x] 퀘스트 창을 화면 오른쪽 고정 + HUD 우측 상단에 목표/의뢰/노트 진행 트래커
- [x] 능력치 창 분리: 인벤토리(I)에서 능력치·펫을 U키 전용 창으로

## ✅ M19. 캐릭터/시작 연출/퀘스트 개편 (완료)

- [x] 신규 플레이어 캐릭터: 참조 시안 기반 24x36 대형 도트
      (더벅머리+아호게, 크림 셔츠, 올리브 조끼, 가방끈, 장갑/부츠)
      NPC도 같은 베이스의 팔레트 스왑으로 통일
- [x] 환경 팔레트를 캐릭터에 맞춰 따뜻한 올리브톤으로 (봄/여름 잔디·나무)
- [x] 새 게임은 물려받은 집 안에서 눈을 뜨며 시작 (편지 스토리 → 문으로 나가기)
- [x] 튜토리얼 보상: 목표 달성마다 돈/씨앗/목재/석재 지급 (해금과 별개)
- [x] 도구 슬롯 자유 배치: 인벤토리에서 우클릭 → 원하는 숫자(1~9) 슬롯과 교환,
      저장됨 (보상으로 받은 도구를 원하는 키에 배치 가능)
- [x] 퀘스트 트래커 박스: 우측 상단 반투명 패널 + [퀘스트 (J)] 헤더

## ✅ M20. 콘텐츠 대확장 2 (완료)

- [x] NPC 3명 추가: 대장장이 무쇠 / 목장주 보라 / 이장 덕수
      (각자 대사 4종 + 호감도 50/100 비밀 이야기 + 초상화, 마을 배회)
- [x] 채집: 아침마다 들판에 산딸기/약초가 돋아남 (E로 채집, 벌목 숙련 경험치)
- [x] 곤충: 나비(봄·여름)/잠자리(여름·가을)/반딧불이(여름 밤)가 들판을 날아다님
      가까이서 E로 포획 — 연구 노트에 채집/곤충 기록 페이지 추가 (+5)
- [x] 세계수 동굴 (깊은 숲): 강화 몬스터(2배) + 3층 보스 '숲의 수호자'
      보스 처치 시 세계수 가지(전설 재료) + 보석 3개
- [x] 수확 품질 등급: 농사 숙련도에 따라 은(1.25배)/금(1.5배) 품질
      (판매가 자동 반영, 요리/의뢰는 일반부터 소비)
- [x] 축사 건설 (목장 상회 5,000G+목재20): 동물 16마리 + 굳은 날씨 자동 배부름
- [x] 오브젝트 비율 개선: 나무 2배(32x48)/바위 1.4배/집 1.4배/동물 1.35배 —
      큰 캐릭터(24x36)와 비율 통일 (충돌 칸은 그대로)
- [x] HUD 다듬기: 상단 코인/하트 아이콘 + 구분선, 하단 도구 칩(아이콘+이름),
      메시지 배경 칩, 퀘스트 트래커 문구 축약("목표: 밭 갈기 (1)")

## ✅ M21. 스타듀식 HUD 개편 (완료)

- [x] 우측 상단 나무 프레임 시계 패널: 날짜/날씨/시각/소지금/체력 바
- [x] 그 아래 퀘스트 트래커 패널 (짧은 목표/의뢰/노트 진행, 같은 나무 프레임)
- [x] 하단 중앙 핫바 12칸: 1~9 숫자키 + 여분 3칸(클릭 전용) — 인벤토리
      우클릭 교환으로 원하는 배치, 선택 슬롯은 붉은 테두리
- [x] 도구 이름은 핫바 위 중앙, 행동 메시지는 그 위에 반투명 배경으로

## ✅ M22. 세계 32px 업스케일 (완료)

- [x] 타일 16px → 32px, 뷰포트 960x540 (창 960/1440/1920 + 전체 화면)
      화면에 보이는 타일 수가 줄어 픽셀 밀도 2배 — 디테일이 살아나는 기반
- [x] 지형 리뉴얼: 잔디(풀잎 다발·들꽃·흙 틈), 밭(흙덩이·고랑 2줄),
      길(자갈), 물(물결 곡선·반짝임)을 32px에서 새로 그림
- [x] 나머지 스프라이트 전부 2배 업스케일 (개별 리디테일은 이어서 진행)
- [x] 캐릭터 2배 표시(화면상 64x96), 이동/반경/몬스터 속도 전면 보정
- [x] 동굴 타일 32px, 집 내부/전 UI 1.5배 재배치, 폰트 22px
- [x] 구버전 세이브 자동 환산 (플레이어/동물 좌표 2배, 가구 1.5배)

## M23. 사용자 원본 도트 캐릭터 적용 (완료)

- [x] 사용자가 제작한 AI 도트 4장(정면/뒷면/옆걷기 2장, `game/assets/ref/`)을
  128x192 픽셀 격자로 정규화 (배경 제거, 박스-모드 다운샘플, 옆모습 미러)
- [x] 파생 프레임 5장 제작 — 옆 서기(다리 모으기), 앞/뒤 걷기(다리 세로 압축)
- [x] 엔진 3프레임 구조 확장: 대기(idle) / 걷기 0 / 걷기 1 — 방향별 9텍스처
- [x] 표시 스케일 0.5 적용(월드 크기 64x96 유지) — 1080p 전체화면에서 원본 1:1
- [x] gen-sprites에서 플레이어 생성 제외 (원본 PNG 보호)
- [ ] NPC를 새 캐릭터 스타일로 교체 (팔레트 스왑 또는 추가 생성)

## M24. 맵 2배 밀도 + 4박자 걷기 (완료)

- [x] 지형 타일 네이티브 64px 재생성 (잔디 결/들꽃, 흙 알갱이/고랑, 자갈길, 물결)
- [x] 농장 오브젝트·작물 Scale2x(EPX) 2배 (나무/바위/집/울타리/작물 등 — 계단 완화)
- [x] 타일은 칸에 맞춰 그리기(draw_texture_rect), 오브젝트는 절반 스케일 —
  월드 좌표·세이브 완전 호환, 1080p 전체화면에서 아트 1:1
- [x] 옆 서기 원본(5번) 교체 + 4박자 걷기 사이클 (걸음A→서기→걸음B→서기)
- [x] 동굴 렉트 드로잉 보정, 핫바/인벤 아이콘 expand_icon

## M25. 여자 캐릭터 + 성별 선택 + 인벤토리 개편 (완료)

- [x] 여자 캐릭터 4장(`game/assets/ref/f1~f4`) 정규화 + 파생 (player_f_* 9텍스처)
- [x] 새 게임 성별 선택 (남자아이/여자아이), 세이브에 저장
- [x] 인벤토리: 12x4 아이템 그리드 (자원/씨앗/수확물/생산물 자동 채움, 툴팁)
- [x] 도구 드래그&드롭: 인벤토리 ↔ 하단 핫바 자유 배치
- [x] 집 문 캐릭터 1:1 비율 (문 20x30 아트 = 월드 64x96, 집 스케일 0.8)

## M26. 공통 NPC 대화 시스템 (완료)

- [x] 시퀀스 대화 엔진 (`dialog_ui.open_seq`) — 모든 NPC가 공유,
  화자/초상화/대사/선택지/이벤트 데이터만 교체
- [x] 엔트리별 이벤트 콜백 (아이템 지급·퀘스트 시작 등) + 대화 종료 콜백
- [x] ESC 스킵: 텍스트·연출만 건너뛰고 남은 이벤트는 전부 실행
  (InputMap `dialog_skip` 액션 추가 시 컨트롤러/모바일 스킵 버튼 확장 가능)
- [x] E/Space로 다음 대사 진행
- [x] 스토리 연출(오프닝/부지/엔딩)도 ESC 스킵 — 오프닝은 선택 페이지로 점프,
  부지/엔딩은 정상 종료 루틴 유지
- [ ] 우체부 등 신규 NPC 대화 데이터는 open_seq 형식으로 추가

## M27. 메인 스토리 1 「우체부 아저씨와의 첫 만남」 (완료)

- [x] 새 게임은 울창한 숲(홈 부지 동남쪽)에서 시작 — 집터까지 개척하며 이동
- [x] 퀘스트 1 「우거진 숲에 들어가 보자」: 몇 걸음 걸으면 완료
- [x] 스토리 이벤트: 우체부 아저씨가 멀리서 걸어와 말을 건다 (ESC로 연출 스킵 가능)
- [x] 공통 대화 시스템으로 대화 + 나무도끼 지급 (스킵해도 지급 보장)
- [x] 퀘스트 2 「나무를 베어보자」: 나무 1그루 벌목 시 완료
- [x] 퀘스트 창/트래커에 메인 스토리 목표 표시, 세이브 저장(main_story)
- [x] 우체부 NPC 스프라이트/초상화, 구세이브·멀티 게스트 호환 (스토리 없음)
- [x] 퀘스트 2 이후 스토리는 미정 (명세 범위 준수)
- [x] v2: 왼쪽 시작 → 오른쪽 숲 개척 구조, 시작 부지 연못 제거,
  숲 불규칙 밀도 + 큰 돌 + 나무 크기/방향 다양화, 집은 시작 화면에 안 보임
- [x] v2: 나무 3단계 파괴 비주얼 (1타 잎 제거 → 2타 반파 → 3타 제거)
- [x] v2: 우체부는 화면 왼쪽에서 등장해 오른쪽으로 걸어옴
- [x] v2: 끼임 탈출이 미구매 부지/물/맵 밖을 뚫던 이동 버그 수정
- [x] 대화 선택지 시스템은 open_seq의 choices로 이미 지원 (추후 분기 설계)

## M28. 앞모습 걷기 (사용자 60프레임) (완료)

- [x] 60장 정규화(흰 배경 제거, 머리 앵커) 후 핵심 4포즈(f5/10/14/17) 선정
- [x] 남자 앞모습 걷기 4프레임 사이클(6fps) — 옆모습과 동일한 리듬

## M29. 옆모습 신규 43프레임 (걷기 + 서기) (완료)

- [x] 신규 43장 옆 걷기에서 핵심 4포즈(21/25/28/32) 선정 — 원본 그대로, 6fps
- [x] 영상 끝의 멈춰 서는 39~42번 4장을 옆 서기(숨쉬기) idle로 사용 (0.4초/프레임)
- [x] 정면 서기 60장(041~100)에서 숨쉬기 사이클 4포즈(59/62/64/68) 선정
  — 앞모습 idle도 애니메이션 (0.4초/프레임), 원본은 ref/m_idle_front/ 보관

## M30. 메인 스토리 1 초반 퀘스트 개편 — 스토리로 배우는 조작 (완료)

- [x] 퀘스트 1 「숲 안으로 들어가보기」: 숲 안으로 일정 거리 이동 시 완료, "(완료)" 표시
- [x] 우체부 이벤트 후 나무도끼 획득 — 슬롯 장착 전에는 사용 불가
- [x] 퀘스트 2 「나무도끼를 장착해보기」: 가방(I)에서 빠른 슬롯에 도끼를 넣으면 완료
- [x] 퀘스트 3 「나무를 베어보자」: 기존 상호작용(마우스 선택 + E)으로 벌목
- [x] 빠른 사용 슬롯 12칸 → 9칸 (1~9 숫자키), 가방 슬롯에 번호 표기
- [x] 공통 규칙: 도구는 획득 → 가방 → 슬롯 장착 → 숫자키 선택 → 사용
  (`set_tool`이 미장착 도구를 거부) — 스토리 새 게임은 빈 슬롯으로 시작
- [x] 튜토리얼/해금 문구의 고정 슬롯 번호 제거 (자유 배치), 12칸 세이브 → 9칸 호환

## M31. 시작 정리 + 우상단 UI 개편 + 숲/퀘스트 연출 (완료)

- [x] 시작 시 호미/감자 씨앗 미지급 — 도구·씨앗은 스토리/퀘스트로만 획득
- [x] 우측 상단 UI 대폭 축소: 날씨/날짜/시간/돈 컴팩트 패널 ("☀ 맑음 / 봄 1일 · 오전 8:30")
- [x] 퀘스트 트래커 → 픽셀아트 두루마리 (말린 양쪽 축 + 양피지, 최소 문구)
- [x] 미니 사과나무 버그: 원인은 열매 덤불(forage_berry) 도안 — 줄기 달린 나무 모양에
  빨간 열매 6개가 사과나무로 보임 → 낮은 둥근 덤불 + 보라 열매 3개로 재도안
- [x] 사과나무 규칙 확인: 기존 나무 일부만 사과 3개, 정상 크기 (tree_apple 64x96)
- [x] 퀘스트 완료 토스트: "✔ 퀘스트 완료!" 슬라이드 배너 + 효과음
- [x] 보상 획득 토스트: 아이콘 + 아이템 이름 (나무도끼/의뢰 골드/튜토리얼 보상)
- [x] 스토리 중 집/건물류(house·bin·board·sign·barn·cave) 완전 숨김, 완료 시 표시

## M32. 나무 성장/벌목 시스템 + 상호작용 규칙 (완료)

- [x] 사용자 나무 아트 적용 (원본 15장은 ref/trees/ 보관, 5장 사용):
  어린 나무=tree_15 / 다 자란 기본 나무=tree_01 / 1회 벌목=tree_06 /
  2회 벌목=tree_09 / 3회 벌목=제거, 사과나무=tree_13 (사과 3개)
- [x] 공통 스케일 정규화 (tree_01 키 92px 기준, 상대 크기 유지) — 96x96 텍스처
- [x] 나무 성장: 어린 나무는 2일 뒤 성장, 벤 자리엔 다음 날 어린 나무가 돋는다
  (tree_regrow 대기열 + objects의 young/grow 저장)
- [x] 어린 나무는 벌목 불가 ("아직 어린 나무다")
- [x] 상호작용 규칙: 좌클릭=대상 선택(금색 강조), E=선택 대상과 상호작용
  (나무=도끼 벌목, 돌=곡괭이 채광 폴스루), 더블클릭=선택+즉시 상호작용
- [x] 대상 안내 문구 추가 ("E: 벌목 (도끼)" / "E: 채광 (곡괭이)" / "어린 나무")

## M33. 메인 스토리 1 동행 개편 + 퀘스트 UI/문서 (완료)

- [x] 스토리 목적 변경: 나무 베기가 아니라 「우체부와 함께 숲을 지나 마을 도착 +
  이장에게 편지 전달」이 최종 목표 (튜토리얼 = 메인 스토리 1)
- [x] 인트로 선택지에서 「튜토리얼 건너뛰기」 버튼 제거, "튜토리얼" 표기 제거
  (퀘스트 창 [마을 생활 안내]로 개칭, 스토리 완료 후에만 표시)
- [x] 우체부 새 대화 (스펙 대사 반영) + 이름 질문 → 이름 입력 UI →
  닉네임 저장(player_name), 이후 대사에서 닉네임으로 부름
- [x] 도끼를 주며 퀘스트 확인 방법 안내 (우측 상단 요약 + Q 키 상세)
- [x] 우체부 동행 NPC: 스토리 끝까지 주인공을 따라다니고 개척을 기다림,
  스토리 중 저장/로드해도 동행 복원
- [x] travel 단계: 카메라 잠금 해제 + 마을/건물 표시 + 이장 방향 안내 화살표
- [x] 마을 도착 컷신: 우체부가 이장(덕수)에게 걸어가 편지 전달 대화 → 스토리 완료
- [x] Q 키로 퀘스트 창 토글 (J → Q 리바인딩), 두루마리(양피지+말린 축) 스타일
- [x] Q 상세 구조: ① 퀘스트 이름(크게) ② 해야 하는 일 ③ 스토리 + 전체 목록
- [x] 퀘스트 안내 표시 시간 연장 (토스트 4.5초, 스토리 안내 5~6초)
- [x] 퀘스트 기준 문서 game/docs/quests.md 작성 (번호/이름/할 일/스토리)
- [x] 숲 밀도 강화(빈 공간 축소) + 깊이감(뒤 작게/앞 크게) + 돌 크기 랜덤

## M34. 퀘스트 두루마리 정리 + 숲 밀도 완화 + 자동 상호작용 제거 (완료)

- [x] Q 상세 창: 모든 텍스트(이름/할 일/스토리/목록)가 두루마리 안에 표시되도록 수정
  (PanelContainer 여백 밖에 그려지던 말린 축을 패널 자체 draw로 이동, 중앙 배치 + 여백 확대)
- [x] 숲 밀도 완화: 나무 드문드문(빈 공간 ~50%) + 이동 공간 확보,
  흙바닥 자국(path) 드문드문 + 풀/덤불/돌 혼합, 불규칙 배치 유지
- [x] 자동 도구 선택(_auto_select_tool) 완전 제거 — 접근/바라보기만으로
  도구가 바뀌거나 채집되는 일 없음 (도끼 미장착 벌목 버그의 원인)
- [x] use_tool 입구 게이트: 슬롯에 장착 + 직접 선택한 도구만 사용 가능 (맨손 수확 제외)
- [x] 검증용 스토리 하네스에 Q 창 캡처(story_quest) 추가

## M35. 모션 확장 + 퀘1 연출 강화 + 집 이사 (완료)

- [x] 걷기 3방향 8프레임(12fps)으로 확장 (옆 21~34 / 앞 6~22 / 뒤 6~17 균등 샘플)
- [x] 정면 서기 6프레임(0.3초/프레임), 옆 서기 4프레임 핑퐁(6단계) 숨쉬기
- [x] 미사용 원본 프레임 348장 + 구 세트(m_walk 20장, m_idle 5장) 저장소에서 삭제
  (사용 중인 원본만 ref에 보존)
- [x] 퀘1 시작 연출: 숲 앞 독백 — 할아버지의 말 『집으로 가는 길이 조금 힘들 거다』
  회상 (그때는 몰랐지만 이제 이해) → 연출 후 퀘스트 목표 표시
- [x] 퀘1 달성 조건 변경: 걷다가 나무에 정면으로 막히는 순간
  「더 이상 갈 수 없는 길인 것 같다.」 자막 → 완료 → 우체부 왼쪽 등장
- [x] 시작 지역 집 삭제 → 우리집을 마을 서쪽(광장 옆)으로 이사
  (스토리 "집으로 가려면 숲을 지나야 한다"와 일치, 잠자기/집 내부 유지)
- [x] 지도 라벨 정리 (우리집 위치 이동, 없어진 연못 -> 호수), 낚시 안내 화살표 호수로

## M36. 집 건설/침대 제작 + 밤 시스템 (완료)

- [x] 스토리 1 완료 전에는 집이 없다 — 완료 후 마을 서쪽 집터(E)에서 목재 20으로 건설
- [x] 집 업그레이드: 확장(목재 60·석재 40) 시 부엌(요리)·꾸미기(F) 해금
- [x] 침대는 직접 제작(목재 10) — 만들기 전에는 빈 자리만 보이고 잠을 잘 수 없다
- [x] 잠을 자야 하는 자연스러운 이유:
  - 19시: NPC들이 귀가해 만날 수 없다 (이장은 스토리 travel 중 예외)
  - 20시: 채집 패널티 — 도구 사용마다 기력 -4, 숙련도 획득 절반
  - 21시: 야외에 지네 몬스터 등장 (최대 2, 추적+접촉 피해 10, 기력 0이면 쓰러짐)
- [x] 지네 스프라이트 2프레임 자체 제작 (mob_centipede_0/1)
- [x] house_lv/has_bed 저장, 검증 하네스는 집·침대 완성 상태로 촬영

## M37. 폰트 체계 + 걷기 6프레임 + 서기 스케일 숨쉬기 (완료)

- [x] 폰트 규칙: 큰 글씨(14px+)=갈무리11(기본 테마), 작은 글씨(13px 이하)=갈무리9
  (HUD 시계/트래커/토스트/슬롯 번호 등 작은 라벨에 갈무리9 적용, 뭉개짐 해소)
- [x] 월드 오버레이 draw_string도 크기별 분리 (22px=갈무리11, 18px 플로팅=갈무리9)
- [x] 걷기 3방향 6프레임(9fps)으로 통일 (사이클 균등 샘플 재선정)
- [x] 서기 모션: 프레임 애니메이션 제거 -> 단일 서기 프레임 + 세로 스케일 숨쉬기
  (발 고정, 머리만 살짝 오르내림 — 뒷모습에도 적용됨)
- [x] 미사용 프레임 재정리 (방향별 걷기 6장 + 서기 1장만 ref 보존)
- [x] 우체부 보조 대사: 도끼 장착 전(equip)에만 사용법 안내, 이후 단계별 짧은 대사
- [x] 평상복 커스텀 초안 이미지 보관 (ref/outfit_casual_draft.png) — 시스템은 추후

## M38. 퀘4 「지도를 확인해보자」 + 갈림길 + 지도 fog of war (완료)

- [x] 벌목 후 바로 마을로 가지 않음 — path 단계: 나무를 베며 숲길 개척 (화살표 안내)
- [x] 숲 속 갈림길 지형 생성 (북/남/동 세 갈래 흙길, STORY_FORK=20,16)
- [x] 갈림길 도착 시 자동 대화: 「길이 여러 갈래로 나뉘었구먼」 → M 키/지도 안내
- [x] 퀘4 「지도를 확인해보자」: 지도 UI가 실제로 열려야 완료 (키 입력 감지 아님)
  → 닫으면 완료 토스트 + 「마을은 이쪽 방향일세」 대화 → travel 단계
- [x] 지도 fog of war: 가 본 지역(8타일 청크)만 표시, 미방문/미해금 부지는 검은색
  (미해금 부지 이름/가격 미리보기도 제거), 탐사할수록 하나씩 공개
- [x] 내 위치 마커 강화 (금색 펄스 고리 + 깜빡이는 점 + 라벨)
- [x] explored 청크 저장/로드, 검증 하네스는 전체 탐사 상태로 지도 캡처
- [x] quests.md 번호 재정렬 (만남/숲길 개척은 이벤트, 지도=퀘4)

## M39. 평상복 옆모습 + U키 능력치 안내 + 퀘5 「마을로 가는 길을 열어보자」 (완료)

- [x] 평상복 옆모습 적용: 걷기 6프레임 + 서기 1프레임 (총 47장 중 선별)
  - 원본 누끼의 다리/부츠 알파가 지워진 프레임 복원: 고알파 시드에서
    RGB(비검정)로 연결 성장하는 캐릭터 마스크 방식으로 파이프라인 개선
- [x] U키 능력치 안내: 스토리 중 15그루째 벌목 시 우체부 자동 대사
  → U로 능력치 창을 열었다 닫으면 완료 토스트 (u_intro_state 저장)
- [x] 벌목 숙련도 소폭 상향으로 조정 (경험치 6.0 → 3.0, 15그루에 저레벨 유지)
- [x] 퀘5 「마을로 가는 길을 열어보자」: 갈림길 이후 커다란 바위(bigrock)가
  동쪽 길을 완전히 차단 (STORY_ROCK=26,16 + 위아래 나무 벽)
  - 바위 발견 시 자동 대화 → 우체부가 곡괭이 채광 시연 (mine_demo 상태)
    → 낡은 나무 곡괭이 전달 (스킵해도 지급 보장)
  - 채광 규칙 준수: 곡괭이 장착 → 바위 좌클릭 → E (자동 선택 없음), HP 4
  - 바위 제거 시 돌 ×4 → 「잘했네」 → 곡괭이 돌려주기 대화 → 정식 획득 → 완료
  - 완료 후 travel 단계: 새 튜토리얼 없이 마을 도착 → 편지 전달로 스토리 마무리
- [x] 퀘스트 목록 9개로 재편 (rock 단계 추가), story_rock_state 저장/로드
- [x] quests.md 퀘5 전면 개정 + 「마을로 이동」은 이벤트로 재분류

## M40. 마을 맵 전면 재설계 — 건물 0개 + 중앙 광장 + 마을 발전 (완료)

- [x] **마을에서 모든 건물 제거** — 처음 마을에는 건물이 하나도 없다
- [x] 아주 넓은 중앙 광장(14x10) + 한가운데 분수(4x4 물 + 조형물)
- [x] 최소한의 장식만: 가로등 4 · 벤치 4 · 게시판 1 (신규 도트 3종 제작)
- [x] 광장에서 북/남/동/서로 뻗는 방사형 길 + 마을을 가로지르는 큰길
- [x] 건물 부지 8곳을 빈 공간으로 확보 (우체국/잡화점/대장간/연구소/여관/
  도서관/목장 상회/수산시장) — 표지판·이름도 표시하지 않는다
- [x] 마을 안 채집용 나무·돌 전면 제거, 나무는 외곽에만 배치
- [x] 마을 외곽을 따라 흐르는 강(남쪽·동쪽) + 남쪽 끝 낚시터 부두 + 다리
- [x] 우리집 집터를 서쪽 큰길가(61,4)로 이동
- [x] **마을 발전 시스템**: 이장에게 이야기해 재료를 모으면 빈 부지에 건물이
  하나씩 세워지고, 그 건물의 주인 NPC도 그때 마을에 합류한다 (`village_built` 저장)
- [x] 지도: 지어진 건물만 이름 표시, 중앙 광장/낚시터 라벨 추가
- [x] 기준 문서 `game/docs/village.md` 신설

## M41. 평상복 뒷모습 적용 (완료)

- [x] 업로드 40장(041~080) 분석 — 발 높이차로 걷기 주기 12프레임 확인,
  077~080은 정지 자세로 판별
- [x] 걷기 6프레임(061·063·065·067·069·071) + 서기 1프레임(078) 추출
- [x] 원본 알파가 깨끗한 배치라 알파 임계값 마스크 사용 (배경은 밝은 회색)
- [x] 서기 키 677px → 184px 기준 스케일(0.2718)로 다른 방향과 크기 일치
- [x] 평상복 착용 시 뒷모습 걷기/서기가 적용되도록 연결 (`player_casual_up_*`)
- [x] 사용한 원본 7장은 `ref/outfit_casual_up/`에 보관, 나머지 33장 정리
- [x] 검증 하네스에 평상복 뒷모습 걷기 스크린샷 추가

## M42. 평상복 앞모습 적용 — 평상복 4방향 완성 (완료)

- [x] 업로드 60장(041~100) 분석 — 041~059는 정지 자세, 060부터 걷기
- [x] 다리 실루엣 자기상관으로 걷기 주기 **11프레임** 산출,
  한 주기 안에서 프레임 간 변화가 가장 고른 조합을 자동 선택
  (075·077·079·081·083·084) + 서기(050)
- [x] 서기 키 828px → 184px 기준 스케일(0.2222)로 옆/뒤와 크기 일치
- [x] 앞모습 서기를 초안 이미지 대신 이번 배치 프레임으로 교체 (걷기와 톤 통일)
- [x] 평상복 착용 시 앞모습 걷기/서기 적용 (`player_casual_down_*`)
- [x] 사용한 원본 7장은 `ref/outfit_casual_down/`에 보관, 나머지 53장 정리
- [x] 검증 하네스에 평상복 앞모습 걷기 스크린샷 추가
- [x] **평상복 4방향(앞·뒤·좌우) 걷기 6프레임 + 서기 완성**

## M43. 옆모습 재적용 + 크기 통일 + 우체부 도트 + 오브젝트/NPC 손질 (완료)

- [x] 평상복 옆모습 새 원본(45장) 적용 — 자기상관으로 주기 13프레임 산출,
  걷기 6프레임(014·016·018·021·023·025) + 서기(045)
- [x] **4방향 크기 통일 검증**: 어깨~발 몸 높이를 재서 농부/평상복 · 앞/옆/뒤가
  모두 총 184px · 몸 136px로 일치함을 확인 (측정 스크립트로 수치 검증)
- [x] 우체부 아저씨 도트 적용 — 배경(구워진 체커보드) 제거 후 월드 스프라이트
  6종 + 초상화 2종(평상/웃음) 생성, 플레이어와 같은 밀도에 어른답게 12% 크게
- [x] 숲 입구 테두리 나무를 치워 우체부가 걸어오는 장면이 가려지지 않게 수정
- [x] **오브젝트 크기 확대**: 나무 2.0→3.0, 돌 1.4→1.9, 큰바위 3.0→4.0,
  동굴 1.5→2.2, 세계수 1.6→2.6, 채집물 1.2→1.5 (주인공 키 기준으로 재조정)
- [x] **오브젝트 겹침 방지**: 자연물 배치 시 최소 간격 규칙(`_nature_clear`) 도입
  — 일반 지역 나무 2칸, 깊은 숲/숲길/마을 외곽 1칸, 맵 테두리는 한 칸씩 띄움
- [x] **NPC 자동 배회 버그 수정** — 목적지 계산이 옛 16px 타일 기준이라 마을
  NPC가 제자리에 멈춰 있었다. TILE(32) 기준으로 고치고 1~3칸씩 이어 걷게 조정
- [x] **선물을 직접 고르게** — 「선물하기」 선택 시 보유 수확물/생산물 목록에서
  원하는 것을 골라 건넨다 (멀티플레이 동기화 포함)
- [x] 검증 하네스: 스토리 종료를 프레임 수가 아닌 진행 시간 기준으로 바꿔
  우체부 등장/대화 캡처를 안정화, 선물 창 스크린샷 추가

## M44. 줌아웃 + 오브젝트 겹침 완전 제거 + 호미 획득 (완료)

- [x] 카메라 줌 0.8 — 화면에 보이는 범위가 30x17 → **37.5x21 타일**로 넓어지고
  모든 것이 20% 작게 보인다 (`CAMERA_ZOOM`)
- [x] 스토리 숲 구간도 넓어진 화면에 맞춰 카메라 잠금 범위와 숲 폭을 확장
  (숲 가로 30 → 38타일, `STORY_FOREST_W`)
- [x] **오브젝트 겹침 완전 제거**: 실제 그려지는 폭에서 최소 간격을 계산해
  배치한다 (`NATURE_CLEAR`) — 나무 5타일, 큰바위 4, 돌·채집물 3타일 간격.
  두 오브젝트가 요구하는 간격 중 큰 값을 적용해 종류가 섞여도 겹치지 않는다
- [x] 나무 크기 편차 축소(0.86~1.00) — 편차가 크면 간격을 지켜도 그림이 겹친다
- [x] 숲 구간은 4타일 간격으로 촘촘하게(겹치지 않는 한계까지) 세워 숲다움 유지
- [x] 맵 테두리 나무도 4타일 간격으로 정리
- [x] **호미 획득 경로 추가** — 이장이 편지를 받은 뒤 환영 선물로 호미를 건넨다
  (「호미로 땅을 갈아 밭을 만들면…」) → 생활 안내의 밭갈기 목표로 자연히 이어짐
  대화를 스킵해도 지급 보장

## M45. 숲 미로 설계 + 나무 겹침 규칙 재정립 (완료)

- [x] **겹침 규칙을 방향별로 분리** — 나무는 "옆으로 나란히" 놓일 때만 지저분하게
  겹친다. 앞뒤(위아래)로 겹치는 건 앞 나무가 뒤를 가려 주므로 깊은 숲처럼 보인다.
  → 흩어진 나무는 가로 5칸 간격(겹침 0), 숲은 촘촘히 세워 덩어리로 만든다
- [x] **도입부 숲을 퀘스트에 맞춰 직접 설계** (랜덤 배치 → 수동 설계)
  - 시작 빈터(x 3~10, 나무 없음) → 길을 막는 나무 벽(x=11, 세로 전체)
  - 벽 너머는 통로만 남기고 전부 나무로 채운 **미로**
  - 통로 위 세 곳에 나무를 세워, 베어야만 앞으로 나아갈 수 있다
  - 갈림길 → 동쪽 숲길 → 커다란 바위 → 마을 큰길 연결로 (바위를 캐야만 통과)
  - 파이썬 연결성 검사로 "우회로 없음 · 필요한 벌목 3그루 + 바위 1개" 확인
- [x] 빽빽한 숲에서 주인공을 가리는 앞쪽 나무만 반투명 처리
- [x] 농장 지역 나무 밀도 조정 (겹치지 않는 선에서 공원 같은 밀도)

## M46. 숲길 가독성 — 흙길 + 진행도 + E키 우선순위 (완료)

- [x] **숲길을 흙길로 깔아 눈에 보이게** — 빈터부터 마을 큰길까지 하나의 흙길로
  이어져, 어디로 가야 하고 어디까지 뚫었는지가 한눈에 보인다
- [x] **흙길 한가운데 나무 9그루를 간간히 배치** — 베고 넘어가며 나아가는 리듬
  (벽 1 + 미로 3 + 동쪽 숲길 4 + 연결로 1), 커다란 바위까지 우회로 없음
- [x] 남은 나무 수를 우측 상단 목표에 「남은 나무 N그루」로 표시
- [x] 숲길을 막던 나무는 다시 자라지 않게 (길이 도로 막히는 문제 방지)
- [x] **E키 우선순위 수정** — 나무·돌·채집물을 조준 중이면 옆에 사람이 서 있어도
  대화로 새지 않는다 (우체부와 나란히 서서 벌목할 때의 문제)

## M47. 숲길 4줄 + 울타리 + 나무 겹침 완전 제거 (완료)

- [x] **빽빽한 나무 벽을 걷어내고 울타리로 막는 구조로 전환** — 나무끼리 겹치는
  구간이 사라졌다. 길 바깥 숲은 서로 겹치지 않는 간격(가로 5칸·세로 3칸)으로 배치
- [x] **숲길을 4줄 폭 흙길로 확장** (y 15~18, x 4~33)
- [x] **길 양옆·동쪽 끝을 울타리로 차단** — 도끼로 걷어낼 수 없는 고정 울타리
- [x] 길을 통째로 가로막는 **나무 줄 4곳**(x 11·16·22·28)과 **바위 줄**(x 26) —
  베고/캐고 넘어가며 나아간다. 우회로 없음(연결성 검사로 확인)
- [x] 갈림길에서 북·남 갈래를 내되 둘 다 울타리로 막힌 막다른 길 (지도 퀘스트용)
- [x] 진행도 표시를 「남은 길목 N곳」으로 정리, 뚫은 길목은 다시 막히지 않는다
- [x] 스토리 울타리는 저장/불러오기에도 유지된다 (`fixed` 플래그)

## M48. 스토리 연출 잠금 버그 수정 (완료)

- [x] **원인**: 지도 대화 예약(1.4초 타이머)이 뜨기 전에 플레이어가 바위에 도착하면,
  바위 대화가 먼저 열린 뒤 지도 대화가 그 위에 열리면서 **바위 대화의 종료 콜백이
  사라졌다.** 그 결과 컷신 잠금이 풀리지 않아 조작 불가 + 곡괭이 미지급
- [x] 예약 대화는 다른 대화가 진행 중이면 덮어쓰지 않고 기다리거나 건너뛴다
  (`_after_map_dialog` · `_start_travel_dialog`)
- [x] 곡괭이 전달 마무리를 `_end_rock_intro()`로 분리해, 대화가 끊겨도 복구된다
- [x] 전역 안전장치: 대화/입력 창이 없는데 컷신 잠금이 1.5초 이상 남아 있으면
  자동으로 해제한다 (앞으로 어떤 연출이 끊겨도 조작이 잠기지 않는다)

## M49. 부지 구입 시스템 삭제 + 오브젝트 충돌 크기 (완료)

- [x] **부지 구입 시스템을 통째로 삭제**했다. 맵 전체를 처음부터 자유롭게
  오갈 수 있고, 농사/벌목/채광/낚시에 지역 제한이 없다
  - 표지판 오브젝트 · 구입 대화 · 어둑한 오버레이 · 지도 가격 표시 ·
    부지별 장(章) 스토리 · 퀘스트 창의 「마을 탐사」 항목 전부 제거
  - 저장 키 `owned_parcels` 삭제 (구버전 저장은 그대로 불러와진다)
  - 다음 마을로 넘어가는 이야기는 이후 **메인 스토리**로 연다 (M11 대체)
- [x] 길을 낼 때 그 자리에 이미 있던 나무·돌을 함께 치운다 (`_carve_road`)
  — 스토리 4줄 길 위에 나무가 서서 옆으로 못 지나가던 문제 수정
- [x] **오브젝트 충돌 크기를 그림 크기에 맞췄다** (`OBJECT_PAD`).
  나무·바위 등은 칸 하나가 아니라 그림 밑동만큼을 막아, 캐릭터가
  나무 속으로 파고들어 겹쳐 보이지 않는다
  (한 칸 통로는 계속 지나갈 수 있게 한 변 6px 이내로 제한)

## M50. 남자 캐릭터 전면 교체 (완료)

- [x] 새 원본 6장(앞·옆·뒤 서기 + 앞·옆·뒤 걷기 4프레임)을 받아
      **남자 플레이어 스프라이트 15장을 새로 뽑았다** (`new_boy_*`, 128x192)
  - 크기 기준: **앞머리 덩어리 꼭대기~발바닥 163px**을 여섯 원본에 공통 적용.
    총 높이로 맞추면 삐친 머리 길이 차이 때문에 방향마다 몸 크기가 달라진다
  - 걷기는 시트 안에서 한 배율·한 바닥선, 허리(반바지) 무게중심을 x=64에 고정해
    좌우 흔들림이 없다
  - 안티에일리어싱 없이 굵은 버킷 최빈색으로 축소, 15장이 같은 28색 팔레트 공유
  - 걷기 4프레임 8fps (`player_side_tex` 등), 서기는 기존 숨쉬기 스케일 그대로
- [x] **의상(farm/casual) 테스트 시스템 삭제** — HUD 좌상단 「옷 (테스트)」 버튼과
      `GameData.outfit` 제거. 남자는 한 벌로 통일
- [x] **안 쓰는 아트 정리**: 구 남자 스프라이트 49장(`player_casual_*`,
      `player_{down,side,up}_walk_*`, 구 서기)과 참조 원본
      (`ref/m_walk_*`, `ref/outfit_casual_*`, `ref/1~5.png`) 삭제
- [x] 검증 하네스: 남자 앞/옆/뒤 걷기 스크린샷 3장으로 교체

## M51. 파일 정리 (완료)

- [x] 저장소 루트에 남아 있던 업로드 원본 38장(`001~044.png`, 5.1MB) 삭제
- [x] 안 쓰는 폰트 삭제: `Galmuri11-Bold.ttf`, `unifont_ko.otf` (3.4MB)
      — 실제로 쓰는 건 `Galmuri9.ttf` · `Galmuri11.ttf` 두 개뿐이다
- [x] 참조 원본 정리: `ref/icons`(도끼·목재 아이콘 시안 4.3MB),
      한 번도 쓰지 않은 나무 변형 10장, 다시 만들 수 있는 미리보기 이미지 삭제
- [x] 남긴 참조 원본은 **현재 게임 아트를 다시 뽑을 수 있는 것들뿐**이다
      — `ref/new_boy`(남자 6장 + 변환 스크립트) · `ref/f1~f4`(여자) ·
        `ref/npc`(우체부) · `ref/trees`(실제 쓰는 5종)
- [x] 스프라이트 172장 / 오디오 21개는 감사 결과 **미사용 0건** (전부 쓰이고 있다)
- [x] 초기 웹 프로토타입(`web-prototype/`) 삭제 — Godot 이전이 끝나 역할이 없다

## M52. 우체부 첫 만남 조작 잠김 수정 + 방향별 크기 통일 (완료)

- [x] **퀘스트 2에서 화면이 멈춰 있던 문제 수정.** 우체부가 걸어오는 동안
      컷신으로 조작을 잠가 두어, 말을 걸 때까지 아무것도 할 수 없었다
  - 이제 걸어오는 내내 자유롭게 움직일 수 있다. 우체부는 가까이 와서
    멈춰 서고, 머리 위 「E: 말 걸기」를 보고 **직접 다가가 말을 건다**
  - 플레이어가 멀어지면 다시 따라온다 (멀면 빠르게 걸어온다)
  - 목표 문구도 「우체부 아저씨에게 말 걸기 (E)」로 바꿔 실제 조작과 맞췄다
  - 검증 하네스도 자동 대화 대신 **실제와 같은 경로(다가옴 → 기다림 → E)** 를 탄다
- [x] **남자 캐릭터 상하좌우 크기 통일.** 옆모습 걷기 원본이 다른 원본보다
      몸이 8% 짧게(머리가 크게) 그려져 있어, 앞머리~발바닥 기준으로 맞추면
      옆으로 걸을 때 머리가 8% 커졌다
  - 크기 기준을 **머리 높이(앞머리 꼭대기~목)** 로 바꿨다. 자세와 무관하게
    일정한 부위라, 15장의 머리 크기가 59~62px로 맞는다 (전에는 59~66px)
  - 세로 위치는 발바닥을 y=190에 맞춘다 (시트 안에서는 가장 낮은 발 기준)

## M53. 남자 캐릭터 걷기 원본 교체 (완료)

- [x] 걷기 영상에서 배경을 지운 프레임 110장(앞 40 · 옆 40 · 뒤 30)을 받아
      **한 촬영에서 나온 원본으로 15장을 다시 뽑았다**
  - 다리 벌어짐 폭 / 발 무게중심 / 발바닥 높이로 **주기 분석**:
    앞·뒤 약 9~10프레임, 옆 12프레임이 한 사이클
  - 각 방향에서 사이클을 4등분한 프레임을 골랐다
    (앞 19·21·24·26 / 옆 13·16·19·22 / 뒤 23·25·28·30)
  - 각 폴더 끝의 정지 프레임이 서기 포즈라 그대로 서기로 썼다
- [x] **세 방향이 한 촬영에서 나왔으므로 배율을 하나만 쓴다.** 방향마다 따로 맞추면
      원본에 없던 크기 차이를 오히려 만들어 낸다 (머리 높이 중앙값 기준 0.2874배)
  - 세로는 방향마다 그 방향에서 발이 가장 낮은 프레임을 바닥선(y=190)으로 삼아,
    걸을 때의 자연스러운 위아래 흔들림은 그대로 살렸다
- [x] 이전 원본(외곽선 있는 정지 그림 3장 + 4프레임 시트 3장)은 삭제.
      쓴 프레임 15장만 `ref/new_boy/`에 남기고 업로드 폴더 110장은 정리했다

## M54. 대화창 축소 + E 조작 통일 + 물주기 체크포인트 (완료)

- [x] **대화창을 작게** 줄였다. 645x210 고정 -> 폭 520에 내용 높이만큼만 커진다
      (화면 아래 가운데 고정, 핫바를 가리지 않는다)
  - 글자는 상자 한가운데(가로·세로)에 놓고, 글자 크기도 22 -> 17로 줄였다
- [x] **E 한 키로 통일**: 밭 갈기 · 씨앗 심기 · 물 주기 · 수확 · 낚시도 E로 된다
      (좌클릭과 같은 동작). 맨손이고 앞에 작물도 없으면 아무 일도 하지 않는다
- [x] 퀘스트 창 안내가 「(J)」로 굳어 있던 것을 **실제 키(Q)** 를 읽도록 고쳤다
      (키 재설정을 따라간다)
- [x] **작물 성장 50% 체크포인트**: 절반까지 자라면 멈추고, 물을 한 번 더 줘야
      다시 자란다. 50%에 도달한 뒤에 받은 물만 인정된다
- [x] **편지를 전한 우체부가 광장에 그대로 서 있던 문제 수정.** 스토리가 끝나면
      `_story_update`가 바로 빠져나가 떠나는 연출이 아예 돌지 않았다.
      이제 마을 북쪽 길로 걸어 나간 뒤 사라진다

## M55. 우체부 길찾기 퇴장 + 장비 능력치 기초 (완료)

- [x] **퇴장하는 우체부가 분수·벤치·건물을 뚫고 날아가던 문제 수정.**
      직선으로 걷게 해 두어 지형을 무시했다. 이제 **BFS 길찾기**로 지나갈 수 있는
      칸만 밟아 마을 북쪽 길까지 걸어간 뒤, 길 끝에서 서서히 사라진다
      (`_tile_path()` — 앞으로 다른 NPC 이동에도 그대로 쓸 수 있다)
- [x] **장비 능력치 시스템의 토대** (대장간 확장을 위한 밑작업)
  - 능력치 4종: **위력 / 범위 / 기력 소모 / 행운**
  - `TOOL_STATS` = 도구별 Lv1 기본값 + 강화 1단계당 증가량.
    새 장비를 만들면 이 표에 항목만 추가하면 된다
  - `tool_stats(id)` 하나만 보도록 게임 쪽을 정리했다 —
    범위(`_affected_tiles`) · 위력(벌목/채광 타격) · 기력(밤 소모) ·
    행운(수확 등급)이 전부 이 값을 읽는다
  - 능력치 창(U)에 **[장비]** 항목 추가 (스크롤 가능하게 바꿈),
    대장간 강화 목록에는 다음 단계에서 오르는 능력치를 함께 보여 준다
  - **가방(I)에서 도구에 마우스를 올리면** 그 장비의 세부 능력치가 툴팁에 뜬다
    (이름 옆 강화 단계 + 위력·범위·기력 소모·행운 + 다음 강화로 오르는 값)
  - 검증 하네스에 능력치 창(`_stats.png`)·가방 툴팁(`_inv.png`) 확인 추가

## M56. 장비 능력치 4종을 전부 실제 효과로 연결 (완료)

- [x] **위력** — 나무·돌 타격량(이미 연결) + **동굴 공격력**도 이 값을 읽는다
      (예전에는 도끼 강화 단계를 따로 보고 있었다)
- [x] **범위** — 호미·물뿌리개가 한 번에 다루는 칸 수 (1칸 / 전방 3칸 / 3x3)
- [x] **기력 소모** — 밤에만 걸리던 것을 **도구를 쓸 때마다** 걸리게 했다.
      밤에는 그대로, 낮에는 1/4 (`STAMINA_DAY_MULT`). 맨손 수확은 들지 않는다
- [x] **행운** — 작물 등급(이미 연결) + **목재·석재 추가 산출**(행운 1당 +3%p) +
      **희귀 물고기 확률**까지 반영
- [x] 능력치 창(U)에 각 능력치가 무엇에 걸리는지 안내 4줄 추가

## M57. 카메라 가운데 정렬 · 건물 충돌 · 튜토리얼 순서 · 중복 건설 (완료)

- [x] **맵 끝에서 주인공이 화면 구석에 붙던 문제 수정.** 카메라 제한을 맵 밖
      40칸까지 풀고, `_draw`가 맵 바깥을 **어두운 숲 배경**(풀 + 나무 실루엣)으로
      채운다. 어디에 있든 주인공이 화면 가운데 온다
      (숲 스토리 구간은 기존 제한을 그대로 쓴다)
- [x] **건물 그림 옆으로 걸어 들어가지던 문제 수정.** 집 그림은 5x4칸보다
      크게(양옆 1칸·위 2칸) 그려지는데 그 칸이 비어 있었다.
      `art_block`(그림 없는 벽)으로 채운다 — 축사도 같이 적용
- [x] **튜토리얼 순서 정정**: 물 주기 → 수확 → **나무 베기 → 집 짓기 →
      침대 만들기 → 자기** → 돌 캐기 …
      (예전에는 목재를 모으기도 전에 집을 지으라고 했다)
  - 도구 해금도 순서에 맞췄다 (물주기→바구니 / 수확→도끼 / 자기→곡괭이)
- [x] **「짓기」를 연타하면 재료가 계속 빠지던 버그 수정.**
      이장의 마을 건설 · 집 짓기 · 침대 만들기 · 집 확장 모두
      이미 만들었으면 다시 실행되지 않고, 완료 후에는 버튼이 사라진다
      (`dialog.set_buttons()`)
- [x] 개발용 치트를 `DEV_MODE` 한 곳으로 모았다 (소지금 + 기본 아이템 1만 개).
      **출시 전에 `DEV_MODE = false`** 로만 바꾸면 된다

## M58. 인벤토리 리뉴얼 — 탭 분리 + 목록형 (완료)

- [x] 아이콘만 늘어놓던 12x4 격자를 없애고 **탭 + 한 줄 목록**으로 바꿨다
  - 탭 5개: **도구 / 씨앗 / 작물 / 자원 / 요리** — 한 번에 한 종류만 보여 준다
    (아이템이 늘어나도 다른 칸이 밀리지 않는다)
  - 각 줄에 **아이콘 · 이름 · 개수 · 판매가**를 정렬해 마우스를 올리지 않아도 읽힌다
  - 도구 탭은 줄마다 **강화 단계와 위력·범위**를 함께 보여 주고,
    예전처럼 드래그해서 빠른 슬롯/하단바에 넣을 수 있다
- [x] 빠른 사용 슬롯 9칸과 소지금은 탭 위에 항상 고정
- [x] 그림이 없는 아이템은 이름 첫 글자로 대신 표시
- [x] 검증 하네스에 자원 탭 스크린샷(`_inv2.png`) 추가

## M59. 아이템 아이콘 24종 + 수확 도구 삭제 + 문 자동 입장 + 실내 HUD (완료)

- [x] **아이콘이 없던 아이템 24종을 전부 그렸다** (32x32 도트, `ref/new_boy` 방식처럼
      생성 스크립트 보관). 달걀·우유·물고기 4종·광석·보석·요리 10종·채집·전설 재료 등
  - 직접 그린 그림으로 바꿀 때는 **같은 이름의 32x32 PNG로 덮어쓰면** 된다
- [x] **수확은 도구 없이 된다.** 「바구니(hand)」 도구를 없애고, 다 자란 작물은
      무엇을 들고 있든 E/좌클릭으로 딴다 (`_try_harvest`)
  - 수확 기록이 빈 문자열로 남던 버그도 같이 고쳤다 (작물 id를 지우기 전에 저장)
- [x] **문으로 걸어 들어가면 자동으로 입장한다.** 건물 아래 가운데 문 칸을 비워 두고,
      그 칸을 밟으면 집·상점이 열린다 (E도 그대로 된다).
      나올 때 다시 들어가지 않도록 문 앞 한 칸으로 되돌린다
- [x] **동굴·집 안에서도 HUD가 보인다.** HUD 레이어를 실내 화면 위로 올려
      시계·소지금·목표·체력·빠른 슬롯이 계속 보인다

## M60. 가게마다 실내 방 (완료)

- [x] 문으로 들어가면 **가게마다 다른 방**이 열린다 (`shop_room.gd`)
  - 잡화점(선반) · 대장간(화로·모루) · 목장 상회(건초·여물통) ·
    수산시장(생선 상자) · 우체국(우편함) — 벽/바닥 색과 소품이 다르다
  - 방 안을 걸어 다니다가 **계산대 앞에서 E** 를 누르면 그 가게의 거래 창이 열린다
  - 계산대 뒤에는 주인 NPC가 서 있고, 아래쪽 문으로 나가면 마을로 돌아간다
  - 새 가게를 추가할 때는 `ROOMS` 표에 한 줄만 넣으면 된다
- [x] 검증 하네스에 잡화점·대장간 방 스크린샷 추가

## M61. 상점 창 리뉴얼 · 길 정리 (완료)

- [x] **거래 창 UI를 인벤토리와 같은 결로** 맞췄다 (`shop_ui.gd`)
  - 화면 가운데 나무 패널 + 가게 이름 머리글 + 오른쪽에 소지금 표시
  - 탭은 선택/비선택 스타일이 갈리고, 고른 탭에 금색 테두리가 붙는다
  - 사기·팔기·가축·도감·강화 **다섯 탭 전부** 아이콘 + 이름/설명 두 줄 +
    오른쪽 버튼의 같은 줄 모양으로 통일했다
- [x] **길은 걸어 다닐 수 있는 곳에만** 깔린다
  - 건물 그림이 덮은 자리의 흙길을 잔디로 되돌린다 (`_trim_paths_under_building`)
  - 문 앞 한 칸은 남겨 두어 드나드는 길이 끊기지 않는다
  - 이미 지어 둔 세이브도 불러올 때 한 번 정리한다
- [x] 건설 완료 안내창이 버튼 없이 열려 튕기던 문제 수정

## M62. 미니맵 · 낚시터 · 조작 다듬기 (완료)

- [x] **좌측 상단 미니맵.** 내 주변 31x21칸을 항상 띄운다 (`hud.gd`)
  - M키 지도와 같은 안개 규칙 — 가 본 곳만 보인다
  - 지형·건물·사람·동물이 점으로 찍히고 한가운데가 나
  - 실내(집·동굴·가게 방)에서는 자동으로 숨는다
- [x] **퀘스트 두루마리 확대** — 글씨 10 → 14, 폭 160 → 226
- [x] **대화 건너뛰기 버튼.** 대사창 오른쪽 위 `건너뛰기 >>` (ESC와 같은 동작)
  - 남은 대사가 있을 때만 뜨고, 선택지가 남아 있으면 뜨지 않는다
- [x] **낚시터** (마을 남쪽 강가) — 맵에 하나뿐인 낚시 장소
  - 강 위로 뻗은 T자 나무 부두 + 강가 마당 + 표지판·가로등·벤치
  - 둘레의 나무/돌은 배치 후 한 번 정리해 부두와 강을 덮지 않게 한다
  - 「낚시」 목표를 받은 동안에는 이곳에서만 낚싯대를 던질 수 있다
- [x] **가게 실내 배치 정리** — 창문 두 개, 가운데 간판, 굽도리, 계산대 앞 깔개,
      현관 매트. 주인은 간판 아래 계산대 뒤에 선다
- [x] **거래 창에 값을 아이콘으로** — 골드는 동전, 목재는 통나무, 광석은 광석.
      모자라면 붉게 표시된다
- [x] 버그: **물주기가 안 되던 문제.** 덜 자란 작물이 "아직 다 자라지 않았다"로
      물뿌리개 동작을 가로챘다 — 도구가 그 칸에서 할 일이 있으면 도구가 우선
- [x] 버그: **캐다가 말이 걸리던 문제.** E는 캐기와 말 걸기를 겸하므로,
      캐기 시작 후 0.9초는 무조건 도구로 가고, 대화는 바라보는 쪽 사람에게만 걸린다
- [x] 버그: 가게 방에서 주인공 발이 방 밖으로 나가던 문제 (스프라이트 기준점)

## M63. 할아버지의 부탁 · NPC 하루 일과 (완료)

- [x] **할아버지의 부탁** — 기본 안내(튜토리얼)가 끝나면 이어지는 본 게임 길잡이
  - 밭 → 강 → 숲 → 동굴 → 부엌 → 사람 → 노트 절반, 일곱 부탁이 순서대로 온다
  - 부탁마다 할아버지의 편지가 뜨고, 조건을 채우면 보상과 함께 다음 장이 열린다
  - 일곱을 다 들어주면 **마지막 부탁(최후의 연금술)** 이 밝혀진다
  - 두루마리 트래커와 퀘스트 창(Q)이 진행도를 이어서 보여 준다
  - 새 부탁은 `GRANDPA_QUESTS` 표에 한 줄만 더하면 된다
- [x] **NPC 하루 일과** — 시간대마다 갈 곳이 바뀐다 (`NPC_SCHEDULE`)
  - 아침엔 집, 낮엔 일터, 점심 무렵엔 광장, 철수는 낚시터
  - 목적지까지는 길찾기로 걸어가고, 도착하면 그 둘레를 어슬렁거린다
  - 19시에는 기존대로 집에 들어간다
- [x] 검증 하네스에 일과 시각표·부탁 진행 확인과 스크린샷 추가

## M64. 대장간 본체 · 계절 축제 (완료)

- [x] **대장간 제작 탭** — 무기·방어구·장신구 세 부위, 등급마다 셋씩 아홉 가지
  - 재료는 목재·석재·광석·별빛 광석·보석 (동굴에서 캔 것의 쓸 곳)
  - 능력치가 전부 실제로 적용된다: 위력=동굴 공격력 · 방어=받는 피해 ·
    기력 절약=도구 소모 · 행운=품질/추가 수확 · 이동 속도=걷는 속도
  - 가방(I)의 **「장비」 탭**에서 장착/해제, 합계 능력치 확인
  - 능력치 창(U)에도 착용 장비와 각 능력치의 쓸모를 적어 둔다
  - 새 장비는 `GEAR` 표에 한 줄만 더하면 된다
- [x] **계절 축제 4종** — 계절마다 14일에 하루씩
  - 봄 꽃놀이(주민 모두와 인사) · 여름 낚시대회(낚시터에서 5마리) ·
    가을 수확제(작물 출품, 품질이 좋을수록 상금 ↑) · 겨울 별빛제(요리 나누기)
  - 그날은 **NPC 일과를 덮고 모두 축제 자리로 모인다**
  - 광장/낚시터 위에 계절 색 깃발 줄이 걸린다
  - 아침 결산·두루마리 트래커·퀘스트 창이 축제를 알려 준다
  - 새 축제는 `FESTIVALS` 표에 한 줄만 더하면 된다
- [x] 버그: 바위 옆에 서서 다른 쪽을 보면 E가 헛돌던 문제
      (도구에 맞는 대상이 옆에 있으면 그쪽으로 몸을 돌려 잡는다)

## M65. 마을 재배치 · 세 건물 · 광산 깊이 · 온실 (완료)

- [x] **마을 재배치** — 건물을 광장 둘레로 벌리고 채마다 **마당**을 붙였다
  - 마당은 잔디 + 울타리, 문 앞 한 줄만 터 두고 가장 가까운 길까지 흙길이
    저절로 이어진다 (`_build_yard` / `_connect_to_road`)
  - 건물 사이가 붙지 않도록 마을 구역을 남쪽으로 넓혔다 (강·낚시터도 함께 이동)
  - **마을은 처음부터 다 세워져 있다** — 건물을 하나씩 짓는 단계는 없앴다
- [x] **여관 · 연구소 · 도서관** (빈 부지 3곳에 제 일을 주었다)
  - 여관: 100G에 세 시간 쉬면 체력이 가득 찬다
  - 연구소: 씨앗 개량 5단계 — 단계마다 성장 12% 단축 · 판매가 8% 상승
  - 도서관: 아직 못 구한 전설 재료 하나의 힌트
- [x] **광산 깊이** — 최고 도달 층을 기록하고 **5층마다 승강기**
  - 나가면 1층부터 다시이던 문제를 없앴다 (깊은 층 재료를 실제로 모을 수 있다)
  - 동굴 입구에서 시작 층을 고른다
- [x] **온실** (농장 한켠, 목재 150 · 석재 80 · 5000G)
  - 안에서는 계절을 타지 않는다 — 아무 씨앗이나 심고, 계절이 바뀌어도 안 시든다
  - 겨울 작물이 하나뿐이라 겨울이 통째로 비던 문제를 메운다
- [x] 3등급 장비 재료를 전설 「별빛 광석」에서 **「별빛 조각」**으로 분리
      (전설은 하나만 얻히므로 만들 수 없었고, 만들면 엔딩 재료가 사라졌다)

## M66. 월드 확장 · 3줄 인도 · 탈 것 (완료)

- [x] **월드을 넓혔다** — 90x60 → **120x90**. 마을·농장 좌표는 그대로 두고
      남쪽·동쪽에 야생이 붙는다. M키 지도는 맵 크기에 맞춰 배율을 자동으로 잡는다
- [x] **인도를 모두 3줄로** (`ROAD_W`) — 큰길·남북·서쪽·동쪽 인도가 같은 폭이고,
      건물은 이 선에 맞춰 놓았다. 마당 사이 간격도 함께 넓혔다
- [x] **낚시터를 4줄로** — 강 폭을 2줄에서 4줄로 넓히고 부두를 그만큼 내밀었다.
      가로로도 늘려 데크 24칸 · 부두 셋 · 가로등 넷 · 벤치 셋
- [x] **탈 것(말)** — 목장 상회에서 8000G. **F로 탄다**
  - 타면 이동 속도 2.3배. 탄 동안에는 도구를 쓸 수 없다 (F로 내린다)
  - 실내·동굴로 들어가면 자동으로 내린다
  - 내린 자리에 말이 세워지고, 그 자리는 저장된다
- [x] 버그: 지도의 배경/그리기 Control이 마우스를 먹어 끌기·휠이 통째로 죽어 있던 문제
- [x] 버그: 커다란 바위 옆 두 칸에서 E가 헛돌던 문제 (도구 대상 탐색 2칸)

## M67. 마을 안내 · 우리집 이전 · 말타기 F (완료)

- [x] **우리집을 광장 남쪽 빈터로 옮겼다** — 앵커 (61,4) → **(71,28)**.
      옛 자리가 우체국 마당과 겹쳐 두 집이 포개져 보였다.
      하네스에 `HOME_CLEAR_OK` 검사를 넣어 마을 마당·광장과 한 칸도 겹치면 바로 잡힌다
- [x] **건물 이름표** — 지붕 위에 나무 팻말로 건물 이름을 띄운다
      (`_draw_building_signs`). 가게 겉모습이 다 똑같아 어디가 어딘지 몰랐다.
      우리집도 지으면 「우리집」 팻말이 붙는다
- [x] **지도 배율 유지** — M으로 닫았다 열어도 확대한 배율과 위치가 그대로다
      (R을 누르면 처음 크기로). 지도에 **농장·축사·온실** 라벨도 추가
- [x] **말타기 키를 F로 분리** (`mount` 액션). E는 캐기·대화와 겹쳐 말이 안 탔다.
      산 말은 이제 **플레이어 옆 빈 칸**에 세워진다 (`_free_spot_near`) —
      정해진 자리가 막혀 있으면 말이 아예 안 생기던 문제
- [x] 축사/동물/말 구매 안내에 **「농장(맵 서쪽)」** 을 밝혀 적었다 (어디 갔는지 몰랐다)
- [x] 버그: y 34~35에 남아 있던 옛 개천이 넓어진 마을을 가로질러
      광장 남쪽과 낚시터를 갈라 놓던 문제 — 개천을 없앴다

## M68. 축사 다시 그리기 · 농장 정리 (완료)

- [x] **축사를 새로 그렸다** (`assets/ref/make_barn.js`) — 감브렐 지붕 · 두짝문 ·
      다락문 · 창문 · 주춧돌. 크기도 3.6 x 2.7칸 → **7 x 5.5칸**으로 키웠다.
      마을 건물(5 x 4칸)보다 커서 이제 진짜 헛간처럼 보인다
- [x] 축사 자리를 (10,3) → **(10,6)** 으로 내렸다 (그림이 맵 위로 삐져나가지 않게).
      그림이 덮는 칸은 `_block_barn_art()`가 전부 막는다 (`BARN_ART_OK` 검사)
- [x] **출하 상자를 없앴다** — 커진 축사 그림 아래 깔려 잡동사니처럼 보였고,
      마을 잡화점의 「판매」 탭과 하는 일이 같았다. 판매는 잡화점 하나로 모았다
      (오브젝트·아이콘·단축키 분기까지 전부 걷어냈다, `NO_BIN_OK` 검사)
- [x] 버그: **산 말이 농장에 없던 문제.** 말은 목장 상회 **실내**에서 사는데
      「산 자리 근처」에 세우다 보니 마을 한복판에 놓였다. 이제 항상
      축사 앞마당(`HORSE_HOME`)에 선다. 안내문도 F키로 고쳤다
- [x] 세워 둔 말을 **M키 지도에 「말」** 로 표시한다 — 어디 뒀는지 잃어버리지 않는다
- [x] 옛 세이브 정리(`_migrate_farm_layout`): 옛 자리의 상자·축사를 지우고
      새 자리에 다시 놓는다. 새 축사 그림 안에 서 있던 세이브는 밖으로 꺼낸다
      (`MIGRATE_OK` 검사)
- [x] 농장 기준 문서 `game/docs/farm.md` 추가

## M69. 상점 여는 길을 E 하나로 (완료)

- [x] **B키(`open_shop`)를 없앴다.** 가게는 문으로 들어가 계산대 앞에서 E로만 연다
  - B는 네 가게(잡화점·목장 상회·대장간·수산시장)만 알고 있어서
    나중에 넣은 여관·연구소·도서관·우체국은 아예 열리지 않았다 — 되다 안 되다 하는 키
  - 밖에서 거래 창을 바로 띄우다 보니 공들여 꾸민 **실내와 주인 NPC를 건너뛰었다**
  - 가게를 하나 넣을 때마다 `ROOMS` + `_near_shop` + B키 분기를 같이 손봐야 했다.
    이제 `shop_room.gd`의 `ROOMS`에 한 줄만 넣으면 끝난다
- [x] `_near_shop()` 제거 · 키 설정 목록에서 삭제 · 안내 문구 정리
      (HUD 씨앗 없음, 민지 대사, 튜토리얼 마무리 안내)
- [x] B는 빈 키로 돌려 두었다 (나중에 쓸 자리)

## M70. 연금술 조합대 — 레시피를 「발견」한다 (완료)

서사만 있고 손으로 할 것이 없던 자리(연금술 연구자 할아버지 · 연구 노트 ·
전설 재료 7종)를 실제 플레이로 채운다. 자세한 규칙은 `game/docs/alchemy.md`.

- [x] **다섯 속성** (빛/물/불/흙/생명). 재료 24종에 0~3씩 붙였다 (`REAGENTS`)
- [x] **조합대** (집 안, E) — 재료 **3개**를 올리면 속성을 합산해 결과가 정해진다.
      조건을 넘는 조합법이 여럿이면 까다로운 쪽이 이긴다
  - 속성은 **숨기지 않는다.** 재료마다 색 점으로 보여 준다 — 찍기가 아니라 풀이
  - 맞히면 조합법을 알아내고 연구 노트에 기록된다. 틀리면 탁한 앙금만 남는다
- [x] **물약 7종** — 원기(체력) · 행운 · 바람(속도) · 불꽃(공격) · 수호(방어) ·
      성장(작물) · 달빛(밭 전체 물주기). 약효는 그날 밤까지 (능력치 U에 표시)
  - 약효는 새 호출 지점을 만들지 않고 기존 보정 함수에 얹었다
- [x] **조합법 드랍** — 나무 3% · 돌 3% · 커다란 바위 12% · 몬스터 6%.
      모르는 것이 남아 있을 때만 나오고, 주우면 바로 알게 된다.
      실험으로 맞히는 길과 돌아다니다 줍는 길, 두 갈래를 함께 둔다
- [x] **마지막 연금술을 조합대로 옮겼다** — 연구 노트 안에 있던 버튼을 없애고
      연금술은 조합대 한 곳에서만 한다 (노트는 기록하고 가리키기만)
- [x] 물약 아이콘 8종 (`make_potion_icons.js`) · 연구 노트 연금술 페이지
- [x] 하네스: `ALCHEMY_REACHABLE_OK`(모든 조합법을 실제로 만들 수 있는가) ·
      `ALCHEMY_BREW` · `POTION_BUFF` · `POTION_CLEAR_OK` · `RECIPE_DROP_OK` ·
      `FINAL_ALCHEMY_OK`

## M71. 지도 끌기 버그 · 스프링클러 상시 급수 (완료)

- [x] **버그: 지도를 끌면 화면 밖으로 밀려 나갔다.** 이동 한계가 「지도 크기의
      절반」이라, 기본 배율에서도 지도가 한쪽으로 확 밀려 절반이 빈 화면이 됐다.
      이제 **지도가 화면보다 크면 가장자리가 화면 안으로 들어오지 않게,
      작으면 화면 밖으로 나가지 않게** 묶는다 (`MAP_PAN_LIMIT_OK` 검사)
- [x] 끌기를 끝내는 길을 세 겹으로 — `_input`(GUI보다 먼저 오는 「놓기」) ·
      매 프레임 실제 버튼 상태 확인(창 밖에서 놓는 경우) · 창 포커스 상실.
      이벤트 하나만 믿으면 손을 뗀 뒤에도 지도가 마우스를 따라다닌다
- [x] **지도를 여는 동안 HUD를 감춘다** — 미니맵·시계가 큰 지도 위에 겹쳐 보이던
      것도 없애고, 핫바 버튼이 마우스를 먹을 여지도 없앤다
- [x] **스프링클러가 계속 물을 준다** — 예전에는 아침에 한 번뿐이라 방금 설치한
      것도, 낮에 새로 간 밭도 다음 날이 되어야 젖었다.
      설치 즉시 + 성장 틱마다 다시 적신다 (`SPRINKLER_NOW_OK`/`SPRINKLER_KEEPS_OK`)

## M72. 날씨가 하루를 바꾼다 · 울타리 목초지 (완료)

### 날씨 (`game/docs/weather.md`)

- [x] 날씨를 **표 하나**(`WEATHERS`)로 옮기고 셋 → **여섯**으로 늘렸다.
      계절에 맞는 것들 중 무게로 뽑는다 (날짜 해시라 저장이 필요 없다)
- [x] **안개** — 들판 채집물 12개 → 26개. 화면은 가장자리로 갈수록 짙어진다
- [x] **폭풍** — 부러진 가지(목재 12~30)를 얻지만 작물 25%가 주저앉는다.
      비는 오므로 물주기는 쉰다. 굵은 빗줄기 + 번개 섬광
- [x] **별밤** — 반딧불이(빛 속성 재료)가 계절을 가리지 않고 3 → 9마리.
      **연금술의 빛 재료 병목을 여는 날**이다. 밤이 되면 별이 반짝인다
- [x] 「젖는 날」·「궂은 날」을 표에서 읽게 정리 — 예전에는 "맑음이 아니면"
      전부 궂은 날이라 안개·별밤에도 축사가 동물을 먹였다
- [x] 하네스 `WEATHER_ALL_OK` — 1년을 돌려 **여섯 날씨가 다 나오는지** 확인한다

### 울타리 목초지

- [x] **빈틈없이 둘러싸면 목초지**가 된다. flood fill 하나로 판정하므로
      울타리 모양은 아무래도 좋다 (`_recount_pasture`)
- [x] 목초지 안의 동물은 아침마다 **알아서 배부르고**, 생산물이 35% 확률로
      하나 더 나오며, 황금 달걀 확률이 3% → 9%
- [x] 울타리를 놓다가 다 닫히는 순간 「목초지 완성!」이 뜬다
- [x] 하네스 `PASTURE_OK` — 두르면 갇히고, 한 칸 걷어내면 풀린다

### 그 밖

- [x] 하네스 단계 번호가 **또** 겹쳐 검사 하나가 죽어 있었다 (세 번째다).
      시퀀스 머리에 경고를 박고, 겹침 확인 방법을 적어 두었다

## M73. 다시 할 이유 만들기 — 의뢰·동굴·낚시 (완료)

컨텐츠가 없는 게 아니라, 한 번 보고 나면 **다시 할 이유**가 약한 것이 문제였다.
세 곳 모두 「같은 것이 반복된다」는 지점을 고쳤다.

### 오늘의 의뢰 (`game/docs/quests.md`)

- [x] 의뢰가 **「제철 작물 N개」 한 종류뿐**이었다. 물고기·광물·채집물·요리·물약이
      다 있는데 하나도 안 쓰였다 → 종류를 표(`QUEST_KINDS`)로 두고 **여섯 가지**로
- [x] 게시판에 **세 건이 붙고 그중 하나를 고른다** (예전엔 주는 것 하나뿐)
- [x] 할 수 없는 의뢰는 안 붙는다 — 요리는 만들어 본 것만, 물약은 조합법을 아는 것만
- [x] 납품은 `ingredient_count`/`consume_ingredient` 한 쌍이 처리한다.
      새 종류를 넣을 때 납품 코드는 손대지 않아도 된다

### 동굴 (`game/docs/cave.md`)

- [x] 모든 층이 **「테두리 + 8% 기둥」으로 똑같이** 생겼었다 → 층 유형 넷
      (너른 굴 · 돌기둥 숲 · 좁은 갱도 · 동공)
- [x] **희귀 방** 셋 — 광맥방(광석 3배) · 이끼방(몬스터 절반 + 약초) ·
      보물방(상자가 처음부터). 2층부터 26% 확률
- [x] **5층마다 미니보스** — 승강기 층이 곧 보스층이 된다
- [x] 미로/동공은 섬이 생길 수 있다 → 입구에서 flood fill한 `reachable` 안에만
      계단·상자·몬스터를 놓는다 (`CAVE_REACHABLE_OK`, 40개 층 검사)

### 낚시

- [x] 예전에는 **한 번 맞히면 끝**이라 붕어든 황금잉어든 손맛이 같았다 →
      물고기마다 **맞혀야 하는 횟수 · 구간 너비 · 커서 속도**가 다르다
      (붕어 1번 / 잉어·메기 2번 / 황금잉어 3번)
- [x] 단계가 오를수록 구간이 좁아지고(×0.82) 커서가 빨라진다(×1.15)
- [x] **한 번은 놓쳐도 만회할 수 있다** (두 번 놓치면 도망). 단계 표시와
      「낚싯대가 휜다!!!」 같은 귀띔을 함께 보여 준다
- [x] 버그: 무엇이 물었는지 모르는 채로 판정이 끝나면 인덱스 오류가 났다 (가드 추가)

## M74. 동굴을 「탐험」으로 — 넓히고 카메라를 붙였다 (완료)

- [x] **한 화면에 다 들어오던 동굴을 넓혔다.** 26 x 13 → **42 x 28**,
      한 층 내려갈 때마다 +2칸 (최대 76 x 52). 화면에는 20 x 11칸만 보인다
- [x] **화면이 주인공을 따라간다** + 그리기 배율 1.5배 (타일·오브젝트가 커진다)
  - 논리 좌표는 예전 그대로 32px다. 확대와 카메라를 **그리기에만** 적용해
    이동 속도·피격 거리 같은 값을 건드리지 않았다 (균형이 흔들리지 않게)
  - 카메라는 동굴 가장자리를 넘지 않는다
- [x] **미니맵** (오른쪽 아래) — 가 본 곳만 드러난다. 벽·광석·계단·상자·입구·내 위치
- [x] **계단을 처음부터 놓는다.** 예전에는 전멸 → 상자 → 계단 순서라, 넓어진 층에서
      마지막 한 마리를 찾아 헤매야 했다. 이제 걸어서 찾으면 내려갈 수 있고
      상자는 그대로 전멸 보상이다
- [x] 하네스 `CAVE_SIZE_OK`(한 화면보다 확실히 넓은가) · `CAVE_STAIRS_OK`
- [x] 버그: `var w1 := cave.GW` — Variant 필드는 `:=`로 타입을 못 정한다.
      main.gd 전체가 파스 실패해 하네스가 타이틀에서 멈췄다 (이 함정 세 번째)

## M75. 캐기 모션 + 앞을 가리는 오브젝트 비쳐 보이기 (완료)

### 캐기 모션

캐릭터 도트에는 「휘두르는」 프레임이 없다 (서기 + 걷기 4프레임뿐).
새로 그리면 사용자 원본과 톤이 어긋나므로, **아트 없이 코드로** 만들었다.
나중에 휘두르기 도트가 생기면 `player._swing_visual`만 갈아끼우면 된다.

- [x] **몸**: 발끝을 축으로 젖혔다가 내리친다. 감아올리기는 느리게, 내리치기는
      빠르게 (`_swing_curve`) — 그래야 「친다」로 보인다
- [x] **도구**: 손 높이에 도구 아이콘을 띄워 호를 그린다. 위를 향해 칠 때는
      캐릭터 뒤로 간다. 도끼 강화 단계에 따라 아이콘도 바뀐다
- [x] **맞는 쪽**: 파편이 튀고 대상이 **맞은 방향으로 흔들린다**.
      커다란 바위는 화면도 흔들린다
- [x] **부서질 때**: 그냥 사라지지 않고 **팍 튀었다가** 사라진다
- [x] **판정은 예전 그대로 즉시.** 눈에 보이는 것(파편·흔들림)만 0.15초 뒤로
      미뤄 내리치는 순간에 맞췄다 — 조작감은 하나도 바뀌지 않는다

### 앞을 가리는 오브젝트

- [x] 나무·커다란 바위는 그림이 여러 칸을 덮어서, 뒤에 서면 플레이어가
      통째로 가려졌다. **가리는 동안만 반투명(35%)** 으로 만든다
  - 충돌 범위를 그림만큼 넓히는 방법도 있지만, 그러면 숲과 바위밭의
    한 칸짜리 통로가 전부 막혀 지나갈 수 없게 된다. 그래서 범위는 그대로 두고
    보이는 쪽을 고쳤다
  - y정렬상 **플레이어 앞에 그려지는 것만** 대상이다 (뒤엣것은 가릴 수 없다)
- [x] 하네스 `SWING_OK` · `SWING_DRAW` · `FADE_OK`, 스크린샷 `_swing.png` · `_fade.png`
- [x] 버그: 흔들리는 도중 다 캐면 노드가 사라지는데, `var n: Node2D = ...`처럼
      **형을 붙여 받으면 대입하는 순간** 오류가 난다 (Variant로 먼저 받아 검사)

## M76. 지형 타일 재작업 + 충돌 범위 · 첫 퀘스트 손질 (완료)

### 바닥·인도 도트 (`assets/ref/make_tiles.js`)

예전 타일은 「단색 + 점 몇 개」라 넓게 깔면 색종이처럼 보였다. 다시 그렸다.

- [x] **흙길** — 다져진 결 + 잔자갈(밝은 윗면/그림자) + 흙 부스러기
- [x] **풀 12장** (계절 4 x 변형 3) — 2x2 덩어리 노이즈 + 풀잎 + 계절 꽃.
      예전에는 변형 하나에 큰 사각 얼룩이 있어 격자로 보였다
- [x] **밭** — 고랑에 그늘/밝은 면을 넣고 흙덩이 추가. 젖은 밭은 광택
- [x] **흙길 가장자리 4장** — 길과 풀이 만나는 직선을 톱니처럼 흐트러뜨린다.
      풀 타일 위에 이웃한 길 방향으로 덧그린다
- [x] 타일은 **이어붙여도 티가 안 난다** — 좌표를 64로 감아 쓰는 해시만 사용
- [x] 버그: 해시가 JS 정수 한계를 넘겨 자갈이 왼쪽 위로 몰렸다 (32비트로 고정)

### 충돌 범위

- [x] 나무·돌 여백을 그림 밑동에 맞춰 넓혔다 (6 → **13**).
      자연물은 4칸/2칸 넘게 떨어뜨려 놓으므로 한 칸 통로가 막히지 않는다
- [x] **세로 여백은 12를 넘기면 안 된다** — 늘어선 것들 사이의 한 칸 틈이 막힌다.
      커다란 바위를 20으로 올렸다가 `BIGROCK_GAP_OK`에 걸려 8로 되돌렸다
- [x] 하네스 `WORLD_PATH_OK` — 농장 시작 자리에서 **광장·낚시터까지 실제로
      걸어갈 수 있는지** BFS로 확인한다 (범위를 넓혔을 때 길이 막히면 잡힌다)

### 첫 퀘스트 짧게

- [x] 길을 막은 나무가 **네 곳 x 네 줄 = 16그루**라 초반이 지루했다.
      **두 곳 x 두 그루 = 4그루**로 줄이고, 길목에서 길이 두 줄로 좁아지게 했다
      (남은 두 줄은 울타리)
- [x] 커다란 바위도 같은 방식으로 **네 개 → 두 개**
- [x] 하네스 `STORY_GATE_OK` — 막는 줄이 길 안에서 **이어져** 있는지 본다
      (떨어져 있으면 사이로 그냥 지나가 버린다)

## M77. 나무가 쓰러진다 — 벌목 모션 (완료)

M75에서 **휘두르는** 동작은 만들었지만, 마지막 한 방에 나무는 그냥 팍
사라졌다. 「벴다」가 아니라 「지웠다」로 보였다. 쓰러지는 동작을 붙였다.

### 밑동을 축으로 넘어간다 (`object_nodes.gd`)

- [x] 세 박자 — **반동**(0.12초, 도끼가 파고든 쪽으로 되젖힘) →
      **넘어감**(0.46초, 중력처럼 `p²`로 점점 빨라진다) → **착지**(약 84도)
- [x] 축은 **밑동**이다. 스프라이트를 돌리면서 `pos = 밑동 - 밑동.rotated(각)`
      으로 되밀어, 도는 내내 밑동이 제자리에 붙어 있다
- [x] **넘어가는 쪽은 도끼질한 사람의 반대쪽.** 내 쪽으로 덮치면 이상하다.
      정면에서 찍어 좌우가 안 갈리면 칸 해시로 정한다 (볼 때마다 바뀌면 안 된다)
- [x] 땅에 닿으면 한 번 튕겼다가 잦아들고, 우듬지가 떨어진 자리에서
      **흙먼지와 잎**이 인다. 화면도 흔들리고 `sfx_chop`을 음높이 0.55로
      낮게 깔아 「쿵」으로 쓴다
- [x] **그루터기**는 새로 그리지 않았다. 나무 도트의 아래 19%만 잘라
      (`region_rect`) 그 자리에 남긴다 — 원래 밑동과 풀숲이라 그대로 읽힌다.
      누운 몸통이 먼저 사라지고 그루터기가 조금 더 남는다

### 때를 맞췄다 (`tool_use.gd`)

M75 주석은 「내리치는 순간에 맞춘다」고 했지만, 실제로 미뤄진 것은
파편과 흔들림뿐이었다. 손상 단계 그림 교체와 제거는 **즉시** 일어나서,
도끼가 아직 머리 위에 있는데 나무가 먼저 사라졌다.

- [x] `swing_at`에 `after` 콜백을 달아 **그림 쪽 일**을 전부 타격 순간으로 옮겼다
      (손상 단계 교체 · 쓰러지기 시작 · 돌이 튀는 것)
- [x] 판정은 그대로 즉시다. 늦추면 0.15초 안에 한 번 더 눌러 목재를 두 번
      받을 수 있다
- [x] **눌림(squash)** — 날이 파고드는 프레임에 그림이 9% 납작해졌다 펴진다.
      원점이 밑동이라 우듬지만 내려앉는다
- [x] 도끼질마다 우듬지에서 **잎이 떨어진다** (`leaf` 파티클: 낮은 중력 + 팔랑거림)
- [x] 연타로 흔들림이 겹치면 기준 크기·자리가 밀린다 — 새 타격 전에 되돌린다

### 파티클 (`main.gd` · `renderer.gd`)

- [x] `PARTICLE_DEFS`를 배열에서 **사전**으로 바꿨다. 크기·수명·팔랑거림처럼
      종류마다 다른 값을 이름으로 붙인다 (`leaf` · `dust` 추가)
- [x] `spawn_burst(월드좌표, ...)` — 칸 한가운데가 아닌 우듬지에서도 터뜨린다
- [x] `Sound.play_sfx`에 `pitch_base` — 같은 소리를 굵게/가늘게 쓴다

- [x] 하네스 `FELL_START_OK` · `FELL_DOWN_OK` · `FELL_PIVOT_OK`,
      스크린샷 `_fell.png` (반동 / 넘어가는 중 / 착지를 한 컷에)
- [x] 하네스 함정: **389 이후 번호로는 화면을 찍을 수 없다.** 389의 성능 측정이
      `m._process`를 스무 번 돌려 하네스를 그만큼 미는데, 그 사이에는
      프레임이 그려지지 않는다 (392에 넣었다가 엉뚱한 화면을 찍었다)

## M78. 휘두르는 동작 — 완급·상체 분리·잔상 (완료)

M77에서 「맞는 쪽」은 살아났는데 「때리는 쪽」이 그대로였다. 캐릭터 도트에
휘두르는 프레임이 없어서, 몸을 통째로 젖히고 도구 아이콘을 돌리는 게 전부였다.
도트 없이 갈 수 있는 데까지 밀어붙이고, 도트가 생기면 바로 꽂히게 자리를 뚫었다.

### 완급이 먼저다 (`player.gd`)

- [x] **내리치는 정점을 `main.HIT_AT`에 정확히 맞췄다.** 예전 곡선은 정점이
      0.17초인데 판정은 0.15초라, 도끼가 아직 내려오는 중에 나무가 맞았다.
      이제 `_swing_curve`가 두 상수에서 정점 위치를 직접 끌어온다
- [x] **예비동작** — 감아올리기를 길고 느리게(정점까지의 62%), 내리치기는
      나머지 38%에 가속으로. 이게 있어야 「친다」로 보인다
- [x] **히트스톱** — 닿는 순간 정점에서 잠깐 박힌 채 멈춘다
- [x] 되돌아오기는 smoothstep으로 천천히

### 몸을 가른다

- [x] **허리(136행)를 축으로 상체만 감았다 내리친다.** 다리는 붙박이다.
      예전처럼 발끝을 축으로 통째로 젖히면 메트로놈처럼 보인다
- [x] 자른 자리는 상체를 9행 더 아래까지 그려 덮는다 (안 그러면 허리에 계단이 진다)
- [x] **무게 이동** — 감을 때 뒤로, 칠 때 앞으로 몇 px 쏠린다.
      `Node2D.position`이 아니라 스프라이트만 옮긴다 (그림자와 판정은 제자리)
- [x] **눌림** — 닿는 순간 몸이 6% 주저앉는다
- [x] 말 위에서는 가르지 않는다 — 안장에 앉히느라 이미 몸을 잘라 놨다

### 도구

- [x] **호의 방향이 거꾸로였다.** 감을 때 도구가 발치로 내려가고 칠 때 머리
      위로 올라갔다. 이제 감으면 어깨 뒤, 치면 발치다
- [x] 감는 동안은 도구를 **몸 뒤에** 그린다 (어깨 너머로 넘긴 것이니까)
- [x] 방향별 손 위치·호 폭을 표(`SWING_POSE`)로 뺐다
- [x] **잔상** — 날 끝이 지나간 자취를 6개까지 옅게 이어 그린다.
      빠른 구간에서만 자취가 벌어져서, 내리치는 순간에만 눈에 띈다

### 휘두르기 도트가 생기면 (아직 없다)

캐릭터는 걷기 영상 15프레임을 도트로 변환해 만든 것이라(`make_sprites.js`),
팔이 몸통 실루엣에 붙어 있다. 코드로는 「굽히기」까지가 한계고 「휘두르기」는
원본이 있어야 한다. 그 자리를 미리 뚫어 두었다.

- [x] `make_sprites.js`에 swing 세트 — `<방향>_swing_0..2.png`를 폴더에 넣고
      다시 돌리면 그대로 뽑힌다. 세 장이 다 있어야 한 방향이 켜진다
- [x] **크기·바닥선 기준은 걷기·서기 15장으로만 잡는다** — 나중에 원본을
      넣어도 이미 뽑아 둔 15장이 밀리지 않게
- [x] `main._load_textures`가 `ResourceLoader.exists`로 **있으면 쓴다**.
      `TEXTURE_NAMES`에 넣으면 없을 때 터지므로 따로 챙긴다
- [x] 도트가 있으면 `_swing_frame`이 갈아끼우고 몸 회전은 0.34 -> 0.14로 준다
      (도트가 이미 자세를 가지고 있으니 덜 굽혀야 한다)
- [x] **세 장이 다 있을 때만 켠다.** 손으로 한 장씩 그려 넣으면 그 위상에서만
      도트가 나오고 나머지는 서기 자세로 튀어 휘두르다 말고 깜빡였다
- [x] **도면** `docs/swing_guide.png` (`make_swing_guide.js`) — 3방향 x 3위상,
      칸 하나가 정확히 128x192다. 서기 자세를 어둡게 깔고 발바닥선·허리선·
      엉덩이 중심에 **손잡이 끝이 와야 할 자리**를 찍어 준다. 그리는 법은
      `docs/character_swing.md`
- [x] 도구는 도트에 그리지 않는다 — 게임이 손잡이 끝을 축으로 따로 얹는다.
      도끼·곡괭이·호미·물뿌리개가 도트 한 벌을 나눠 쓴다

- [x] 하네스 `SWING_TIMING_OK` (시작 0 / 다 감음 -1 / 타격 +1 / 끝 0) ·
      `SWING_SPLIT_OK` (상체만 돌고 다리는 각 0)

### 휘두르기 도트를 원본에서 뽑아 봤다 (`make_swing_src.js`) — 넣지 않음

**결론부터: 서 있는 그림을 기하학적으로 바꿔서는 자연스러운 동작이 안 나온다.**
회전도 해 보고 새로 그려도 봤는데, 팔만 붙여 넣은 티가 난다. 자연스러운
동작은 프레임마다 실루엣·무게·겹침을 사람이 정해야 나오는 것이라,
서기 한 장에서 계산으로 만들어 낼 수 있는 게 아니었다.

만든 도트는 **게임에서 뺐다.** 어색한 팔을 보여주느니 팔을 안 건드리는
편이 낫다 — 코드가 상체를 굽히고 도구가 호를 그려 대신한다. 생성기와
도면은 남겨 뒀다. 진짜 휘두르는 프레임이 생기면 그때 넣으면 된다.

아래는 해 보면서 알게 된 것들이다 (다시 붙일 때 쓸모가 있다).


원본에서 **팔을 지우고 새로 그린다.** 원본이 도트보다 3.5배 커서 여기서
그리면 계단이 축소에 묻히고 팔레트도 알아서 맞춰진다.

- [x] 처음에는 팔을 **떼어 회전**시켰다. 이음매는 붙었지만 통짜 막대처럼
      보였다 — 매달린 팔을 통째로 돌리면 팔꿈치가 안 접히고 소매도 길게
      늘어난 채 따라 돈다. **지우고 그리기**로 바꿨다
- [x] 이 도트는 평면 색 + 외곽선이라 「굵기가 변하는 선분 + 둥근 끝」으로
      그리면 같은 결이 나온다. 대신 자세를 마음대로 잡는다 — 팔꿈치를 접고,
      소매는 짧게, 주먹은 둥글게
- [x] 지우기: 씨앗을 놓고 외곽선에 막힐 때까지 번진다. 팔이 챙겨 가는
      외곽선은 3px만 (5px면 반바지와 나눠 쓰는 선까지 떼어 가 허리가 파인다)
- [x] 그래도 남는 **가느다란 검은 선**(나눠 쓰던 외곽선의 바깥쪽)을 걷어낸다.
      단 **팔이 있던 자리 안에서만** — 온 그림에 돌렸더니 머리카락과 부츠가
      통째로 갉였다
- [x] **팔은 몸통 앞을 덮고 있다.** 그냥 지우면 그 뒤 몸통까지 없어져 상체가
      반쪽이 됐다 (옆모습 셔츠 폭 31px -> 15px). 지운 자리를 옆에 남은 몸
      색으로 가로 방향으로 메운다 (세로로 번지면 셔츠가 반바지로 샌다)
- [x] 색은 팔레트(28색)에 **실제로 있는 값**만. 중간 색을 지어내면 k-means가
      새 칸을 잡아먹어 다른 색이 밀린다
- [x] 팔 외곽선은 원본보다 굵게(7px) — 3.5배 축소 뒤에 몸통 선과 같아진다
- [x] 호가 **머리 위를 지나게** 잡았다. 0을 지나가면 팔이 몸 옆으로 내려와
      서기 자세로 보인다
- [x] 도구는 위상마다 **주먹이 실제로 가 있는 자리**(`SWING_HAND_DOT`)에 얹는다.
      값은 생성기가 팔 자세에서 계산해 찍어 준다 — 그림과 어긋날 수 없다
- [x] **도끼 그림만 자루 방향이 반대였다.** 곡괭이·호미는 자루가 왼쪽 아래에서
      머리가 오른쪽 위인데 도끼는 그 반대라, 오른쪽을 보고 휘두르면 날이
      등 뒤를 향했다. 가방 아이콘은 그대로 두고 휘두를 때만 뒤집는다
      (`TOOL_MIRROR`)

### 아직 안 한 것

- [ ] **진짜 휘두르는 프레임.** 걷기 15장을 뽑은 그 출처에서 방향당 3장씩
      받아 오는 것이 유일하게 확실한 길이다. 같은 거리·조명만 지키면
      `make_sprites.js`가 크기·바닥선·팔레트를 맞춰 준다.
      코드 쪽 자리는 이미 다 뚫려 있다 (`docs/character_swing.md`)
- [ ] **동굴 전투의 「때리는 모션」** — 지금은 노란 네모 하나다 (`cave_ui.gd`).
      동굴은 자기 캔버스에 직접 그리는 별도 화면이라 이 코드를 그대로 못 쓴다

## M79. 캐릭터 도트 2세대 — AI 시트 한 장에서 18프레임 (완료)

AI로 뽑은 캐릭터 시트(앞/옆/뒤 x 서기1 + 걷기5) 한 장을 잘라 넣었다.
1세대는 걷기 4프레임이었는데 시트에서 5장이 나와 그대로 쓴다.

- [x] 시트에서 칸을 **자동으로** 잘라낸다 (행 밴드 -> 열 밴드)
- [x] 네 줄의 **바닥선을 한 자리로** 모은다. 줄 안의 위아래 흔들림(걸을 때
      몸이 오르내리는 것)은 그대로 남긴다. 방향마다 따로 잡으면 방향을 바꿀 때
      캐릭터가 위아래로 튄다
- [x] **서기 3장이 걷기보다 12% 크게 그려져 있었다** (머리 높이 79 vs 71).
      그대로 쓰면 멈출 때만 캐릭터가 커진다. 0.899배로 줄여 맞췄다
- [x] 이 캐릭터는 머리가 몸에 비해 커서(등신 2.8) `HEAD_H` 59.5를 그대로 쓰면
      키가 196px이 되어 192 캔버스 위로 잘린다. **55.9**로 낮춰 1세대와
      화면에서 같은 키(184px)로 맞췄다
- [x] 걷기 5프레임: `game_data`의 `% 4` -> `% 5`, `TEXTURE_NAMES`에 walk_4 추가
- [x] 생성기는 `assets/ref/new_boy2/make_sprites.js` (1세대는 그대로 둔다)

### 머지가 조용히 죽인 것 두 개

기본 브랜치를 합치는데 **충돌 표시 없이** 두 가지가 망가졌다. 둘 다 어서션과
검사 도구가 잡았다 — 안 돌렸으면 그대로 나갔다.

- [x] `_load_textures`에서 양쪽이 같은 자리에 넣은 줄을 git이 **둘 다 날렸다**
      (기본 브랜치의 FORAGE_IDS 로딩 + 이쪽의 휘두르기 도트 로딩)
- [x] 하네스 단계 번호 341~343이 겹쳐 **나무 쓰러지는 테스트가 죽었다.**
      `tools/check_steps.py`가 잡는다. 262~299의 빈 자리로 옮겼다

## M80. 휘두르는 도트 — 시트 한 장에서 캐릭터 통째로 (완료)

M78에서 「휘두르기 도트가 생기면 켜진다」고 자리를 뚫어 뒀는데, 그 도트가
왔다. AI 시트 **6줄 x 5칸 = 30장**(걷기 3방향 + 휘두르기 3방향)에서 걷기·
서기·휘두르기를 통째로 뽑았다. 생성기는 `assets/ref/new_boy3/make_sprites.js`.

**걷기만 2세대에 두고 휘두르기만 새로 가져오면 안 됐다** — 3세대 캐릭터가
머리가 더 커서(걷기 머리 높이 67 vs 2세대 71) 칠 때마다 머리가 커진다.
그래서 30장을 한 판에서 다 뽑아 갈아 끼웠다.

- [x] 시트 자르기부터 도트까지 **한 파일에서** 한다. 2세대는 손으로 자른 뒤
      스크립트를 돌렸는데, 자르기 값이 어디에도 안 남아 다시 뽑을 수가 없었다
- [x] 위상 **네 장** — 감기 시작 / 다 감음 / 내리침 / 되돌아옴.
      `swing_phase`를 `swing_c`(값)가 아니라 **진행도(시간)** 로 가른다.
      값으로 가르면 감을 때와 되돌아올 때가 같은 값을 지나 한 위상이 두 번
      나온다 (팔이 갔다가 되짚어 오는 것처럼 보인다)
- [x] 내리친 그림은 판정 순간부터 **남은 시간의 절반 넘게** 붙든다.
      히트스톱(0.03초)만큼만 보이면 제일 중요한 자세가 두 프레임에 지나간다
- [x] 도트가 있으면 **몸을 안 가른다.** 그림이 이미 굽힌 자세라 상체 회전을
      또 얹으면 두 번 굽고, 자른 자리가 그림의 팔을 가로지른다
- [x] 뒷모습만 도구 도는 쪽이 반대다 (`SWING_POSE.spin`) — 등을 보이고 몸을
      비틀어 치는 그림이라 주먹이 오른쪽 위에서 왼쪽 아래로 간다

### 도구가 손에서 떠 있었다

- [x] 회전축이 아이콘 한가운데 아래(16,30) 고정이었는데 **자루 끝은 모서리에
      있다** (도끼 x=25, 곡괭이 x=3). 10~13px씩 떠 보였다. `TOOL_GRIP`에
      도구별로 쥐는 자리를 적고, 좌우로 뒤집을 때는 offset도 반대로 잡는다
- [x] 주먹 자리(`SWING_HAND_DOT`)는 미리보기에 눈금을 얹어 놓고 **눈으로 읽어**
      적었다. 그림을 다시 뽑으면 다시 읽어야 한다 (`docs/character_swing.md`)

### 불똥 떼기

내리치는 칸에는 노란 불똥이 같이 그려져 있다. 게임이 맞는 순간 제 파티클을
뿌리므로 두 번 튀고, 발치에서 옆으로 뻗어 스프라이트 폭까지 잡아먹었다.

- [x] 색으로만 고르면 **꼬리 끝이 주먹 살결과 같은 색**이라 안 갈린다.
      노란 심지를 씨앗으로 삼아 **밝은 픽셀만 타고** 번져 따라간다
- [x] 그랬더니 **주먹 속까지 파먹어 손이 도넛이 됐다.** 「불똥은 얇고 몸은
      두껍다」를 조건으로 넣었다 (배경에서 4px보다 깊으면 몸)
- [x] 불똥은 짧은 획 여러 개로 흩어져 있어 획 하나씩 세면 죄다 두세 픽셀이라
      걸러졌다. 3px 안에 붙은 것끼리 한 무리로 묶어 크기를 잰다
- [x] 도트로 줄이면 실 한 오라기로 붙어 있던 것이 끊겨 티끌로 남는다 — 마저 턴다

### 자리 잡기

- [x] 좌우 기준을 걷기는 허리, 휘두르기는 **밟고 선 자리의 무게중심**으로.
      팔을 뻗거나 굽힌 칸은 허리 띠가 팔다리를 같이 집어 실루엣 한가운데로
      흘러간다 (칸마다 따로 잘라 좌표계도 다르다)
- [x] 몸을 깊이 숙인 칸은 머리가 앞으로 쏠려 128px 폭을 7px 넘긴다.
      넘긴 만큼만 밀어 맞춘다 (그 순간 게임이 `pose.shift`로 실어 주는 양과
      방향이 반대라 거의 상쇄된다). 어느 칸을 밀었는지 로그에 찍는다
- [x] `HEAD_H` 59.6으로 화면에서 2세대와 같은 키(184px)

### 하네스

- [x] `_swing_p0..p3.png` — 네 위상을 한 장씩. **도구가 주먹에 붙어 있는지**는
      수치로 안 보인다
- [x] 위상을 세울 때 동작 길이를 200초로 늘여 붙든다. 0.34초 그대로 두면
      헤드리스에서 한 프레임이 0.1초씩 걸릴 때 **세워 둔 위상이 그려지기 전에
      지나가** 한 칸씩 밀려 찍혔다. 위상마다 두 프레임씩 잡는다
- [x] `SWING_PHASE_OK` (0->1->2->3 한 번씩) · `SWING_SPLIT_OK`를 도트/몸가르기
      **둘 중 하나만** 쓰는지 보는 것으로 고쳤다

## M81. 덩치 줄이기 — 임포트·용량 (완료)

돌아가는 속도는 이미 넉넉했다 (`_process` 157us · 드로우콜 144). 문제는
**들여오는 덩치**였다. Godot이 매번 62MB짜리 참조용 원본까지 다 씹고 있었고,
BGM은 무압축 wav로 47MB였다.

| | 전 | 후 |
| --- | --- | --- |
| 콜드 임포트 | 17.3초 | **10.2초** |
| `.godot` 캐시 | 34MB | **19MB** |
| `assets/audio` | 51MB | **11MB** |
| Godot이 보는 자산 | 128MB | **25MB** |

- [x] `assets/ref/`에 `.gdignore` — 게임은 결과물(`assets/sprites/`)만 읽고
      원본은 생성기 스크립트가 node로 직접 읽으므로, Godot이 통째로 건너뛰어도
      된다. `.import` 파일 69개도 같이 지웠다
- [x] BGM 열 곡을 wav -> ogg (47MB -> 6.3MB). wav는 들여올 때 **어차피 손실
      압축(QOA)** 되므로 ogg 쪽이 용량도 작고 음질도 낫다. 효과음은 짧아서 wav
- [x] 안 쓰는 스프라이트를 찾아봤지만 **0장**이었다 (329장 전부 쓰인다).
      이름이 코드에 안 보이는 35장은 `"mature_%s"` 처럼 조립되는 것들

### BGM 열 곡이 5분의 1 지점에서 되감기고 있었다

- [x] 루프 끝을 `data.size() / 2`(16bit니까 2바이트 = 1프레임)로 셌는데,
      **들여올 때 압축되면 data가 압축된 바이트**라 그 셈이 안 맞는다.
      2분 22초짜리 봄 곡이 28초에서 되감겼다. ogg로 바꾸면서 `loop = true`
      한 줄로 정리됐다
- [x] 하네스 `BGM_LOOP_OK` — 곡마다 루프 구간이 실제 길이와 맞는지 본다.
      **소리만 들어서는 모르는 종류**라 어서션이 필요하다

## M82. 튜토리얼 공간과 세계를 갈랐다 (완료)

문서: `game/docs/tutorial_space_world.md`

- [x] 처음 눈을 뜨는 숲길을 **세계 밖**(격자 y 122~159)의 일회성 공간으로
      옮겼다. 마을·농장과는 한 칸도 이어져 있지 않고, 마을로 오르던
      연결로와 가리개 숲은 없앴다
- [x] 마을로 가는 길은 「걸어서」가 아니라 **전환 연출**이다 — 숲길 동쪽
      끝에 닿으면 화면이 어두워졌다가 마을 어귀에서 밝아진다. 그 사이에
      공간이 닫힌다 (오브젝트·지형·지도의 기억까지)
- [x] 지도(M)·미니맵이 `main.world_rect()` 하나를 기준으로 그린다.
      마을 도착 뒤에는 **처음 시작한 자리도 지나온 길도 그림에 없다**
- [x] 마을이 세계의 시작점. 깊은 숲·너른 초원·남쪽 습지·솔숲 골짜기·
      바닷가 벼랑길은 해당 메인 스토리를 지나야 열린다 (`REGION_UNLOCK`)
- [x] 하네스 스텝 233 `WORLDGATE_OK` · 스토리 하네스 `TUTSPACE_OK`
- [x] `tools/check_steps.py`가 `394, 395, ...`처럼 **여러 번호를 묶은 갈래**도
      센다. 이걸 빼먹은 탓에 394가 두 번 쓰인 것을 오래 놓치고 있었다

## M83. 지도 굽기 160ms → 66ms (완료)

문서: `game/docs/shop_hours_map.md`

- [x] **혼자 할 때는 다시 굽지 않는다** — 지도를 보는 동안에는 세상이 바뀌지
      않는다. 0.5초마다 굽던 것이 끄는 도중의 프레임을 통째로 잡아먹었다
- [x] 「탐사만 했다면 보이는 칸인가」를 미리 굽는다(`_vis_idx`) — 칸마다
      확장 구역 목록을 훑던 것이 사라졌다 (가장 큰 몫)
- [x] 탐사 여부는 청크(4x4) 단위로 줄마다 한 번씩만 묻는다
- [x] 지역빛 없는 맨 잔디는 함수를 부르지 않고 바로 칠한다 (+ 흔들림 표 `_noise`)
- [x] **벤치가 재는 자리가 틀려 있었다** — `_process`를 직접 부르는 단계의
      마지막 줄이 하네스 자신이라, 재는 동안 다음 단계들이 안에서 돌았다.
      한 프레임이 10ms로 읽히고 굽기는 아예 빠졌다. 이제 재는 동안
      시퀀스를 멈추고(`_perf_probe`) 「끄는 동안 한 장」과 「여는 순간 굽기」를
      따로 잰다

## M84. 걸음과 시선 · 흔해진 들판 · 검정 무지 (완료)

문서: `game/docs/forage_npc_map.md`

- [x] 생선구이 그림을 유저 그림(todtjsrndl)으로 — 흰 배경을 가장자리에서
      흘려 지우고 32x32 한가운데에 앉힌다
- [x] **찾아오는 사람이 서 있던 자리에서 걸어온다.** 예전에는 연출이 그를
      먼저 플레이어의 남쪽으로 옮겨, 누가 찾아오든 늘 아래에서 위로 올라왔다.
      걷는 내내 가는 쪽을 보고, 멈추면 플레이어를 마주 본다
- [x] 채집물 상한 12 → 90 (안개 130 · **비 190**) · 잡초 70 → 220 ·
      세계를 지을 때부터 들판에 깔리고, 아침마다 상한까지 채운다
- [x] **비 오는 날은 하루 내내 돋는다** — 게임 시간 25분마다 몇 포기씩
- [x] 울타리를 자동 해금에서 빼고 **잡화점 레시피**로 (가방에 목재가 있을 때만)
- [x] 지도·미니맵의 먹구름을 없애고 **검정 무지** 한 색으로. 잠긴 지역도
      같은 검정이다 (구름 원이 배율에 따라 겉돌던 문제가 여기서 끝난다)
- [x] 통행 차단은 사람·짐승을 가리지 않는다. 밤 몬스터도 걸어 다닐 수 있는
      자리에서만 나온다 (예전에는 잠긴 구역 너머에서 나와 벽에 붙었다)
- [x] Q창·미니 트래커에서 계절 축제 항목 삭제 (축제 자체는 그대로)
- [x] 하네스 스텝 234 `LIVELY_OK`

## M85. 대화키를 F로 (완료)

- [x] `talk` 액션 신설 — **F는 말 걸기 · 쓰다듬기**, E는 나머지 전부
      (벌목·채광·채집·곤충·문·계산대·표지판·밭일)
- [x] F 하나로 말 걸기와 말 타기를 같이 쓴다 — 주위에 사람도 가축도
      없을 때만 `mount`로 넘어간다 (`talk()`이 `false`를 돌려줄 때)
- [x] 나무를 연타하다 옆 사람에게 말이 걸리던 길이 **아예 없어졌다**
      (`aiming_object` 방어 코드도 함께 삭제)
- [x] 안내는 전부 `key_label`을 거친다 — 머리 위 `F: 대화`,
      우체부 첫 만남 문구, 설정 화면 키 목록
- [x] 대화창은 F·E·Space 셋 다로 넘긴다
- [x] 하네스 스텝 236 `TALKKEY_OK` · 문서 `docs/talk_key.md`

## M86. 캐릭터 생성창 · 게임 이름 (완료)

- [x] 게임 이름을 **Little Root**로 (타이틀 · 창 제목 · 엔딩 마지막 카드)
- [x] 「새로 시작」 = 나무판 생성창. 왼쪽 액자 + 오른쪽 고르는 자리,
      여는 동안 타이틀 차림은 물러난다
- [x] **색은 전부 색칠한 네모(스와치)로 고른다** — 이름표가 아니라 그 색
- [x] 피부 6벌 · 머리색 8벌 팔레트 신설 (`APPEAR_SKIN`/`APPEAR_HAIR_COL`).
      테두리·눈·눈썹은 표에서 빼서 얼굴선이 뭉개지지 않는다
- [x] 민머리일 때 머리색 줄이 흐려진다 (눌러도 안 바뀌는 것을 색으로)
- [x] 액자 속 농부가 이따금 눈을 깜빡인다
- [x] **이름·농장 이름을 생성창에서 짓는다** — 지도 이름패·엔딩 통계 연동
- [x] 숲의 이름 입력창 삭제. 우체부는 이미 아는 사이로 통성명 한 마디
- [x] 검사 `CREATOR_OK`(픽셀로 센다) · 문서 `docs/character_creator.md`

## M87. 지도 굽기 · 대화키 손질 · 마을 이름 · 브금 이음매 (완료)

- [x] **지도 어긋남 수정** — 숲길에서 구운 그림이 마을에 와서도 남아
      세계 크기로 늘여 그려지던 버그 (`_need_bake`가 혼자 할 때 영영 false)
- [x] 대화 사거리 48px → **96px(세 칸)** · 40px 안쪽이면 방향 무관
- [x] 말이 곁에 없으면 **F는 아무 말도 하지 않는다** (걷는 내내 뜨던
      「아직 말이 없다」 제거) · 가게 안에서도 F로 주인에게 말을 건다
- [x] 말줄임표가 한 문장을 세 조각으로 쪼개던 것 수정 —
      부호 잇달림·괄호 안 끊김·부호만 남은 줄 셋 다
- [x] **마을 이름을 생성창에서 짓는다** (비우면 「교진」) — 대사·간판·
      지도·안내를 화면에 나가는 길목에서 바꿔 준다 (`localize`)
- [x] 만수 이야기가 「만수와 대화하자」에 멈춰 있던 것 — 말을 튼 뒤로는
      잔 단계(레시피→제작→청소→보고→요리→전달)를 그대로 이어서 보여 준다
- [x] **브금 이음매** — 끝나기 전 2.2초를 겹쳐 틀어 되감기는 소리를 지운다
- [x] **바다는 처음부터 물로 보인다** — 물 위는 걸어 탐사할 수 없어
      지도 남쪽이 영영 검은 구멍이었다 (안개 검사는 뭍만 센다)
- [x] 바다를 **격자 끝(MAP_H)까지 한 칸도 빼놓지 않고** 깐다 — 모래사장에서
      화면 아래로 파란 띠가 끊기고 풀밭이 도로 보이던 것. 닫힌 튜토리얼
      숲길 자리도 바다다 (카메라가 발밑 열다섯 줄 아래까지 비춘다)
- [x] 검사 237 `MAPBAKE_OK` · 238 `TALKUX_OK` · 239 `SEAMAP_OK` ·
      문서 `docs/talk_map_fixes.md`

## M82. 주민 21명을 플레이어와 같은 그림체로 (완료)

주민은 보라(rancher) 도트 **한 벌을 색만 바꿔** 21명을 만든 것이었다.
그래서 실루엣이 전부 같았다 — 대장장이도 아이도 어깨 너비와 머리 모양이
똑같고, 멀리서는 누가 누군지 구분이 안 됐다.

`assets/ref/npc/make_npcs.py`가 **플레이어 생성기를 그대로 불러** 21명을
새로 그린다. 색만이 아니라 **생김새**를 사람마다 다르게 준다:

- [x] 머리 다섯 벌 — 민머리 · 짧은 머리 · 삐죽 머리 · 단발 · 긴 머리.
      단발은 긴 머리에서 어깨 아래 머리채를 걷어 만든다
- [x] 쓴 것 — 두건(대장장이) · 밀짚모자(농부) · 챙모자(우체부·어부) ·
      광부 헬멧(램프 한 점) · 베레모(화가) · 머리꽃(꽃집)
- [x] 수염 세 가지(짧은·덥수룩한·흰) · 안경 · 앞치마
- [x] **아이는 다리에서 네 줄을 덜어** 머리가 커 보이게 한다. 그림을 줄이면
      도트가 뭉개지는데, 행을 빼면 찍힌 점이 그대로 남는다
- [x] 초상화(64x64)도 같은 얼굴에서 뽑는다 — 대화창과 도트가 이제 같은 사람

### 그리면서 걸린 것

- [x] 앞치마를 한 칸 건너 칠했더니 천이 아니라 **바둑판 무늬**가 됐다.
      통으로 칠하고 가장자리·아랫단에만 그늘을 둬야 덧댄 천으로 읽힌다
- [x] 안경을 눈 위아래 테로만 그렸더니 **짙은 눈썹**으로 보였다.
      두 알을 잇는 코걸이 한 줄을 넣으니 그제야 안경이 됐다
      (옆모습은 알이 하나뿐이라 대신 귀 쪽으로 다리를 뻗는다)
- [x] 안경테를 윤곽선과 비슷한 어둠으로 뒀더니 얼굴선에 묻혔다 — 더 밝게
- [x] 머리색을 옷과 같은 계열로 두면 **머리채가 후드처럼** 보인다.
      보라·연화·향이의 머리를 갈색 계열로 되돌렸다

## M88. 「조리대에서 요리를 하자」가 안 지워지던 버그 (완료)

- [x] 목표를 **띄우는 쪽**(`FARM_CHAIN_FLAGS`)과 **체크하는 쪽**(`STORY2_FLAGS`)이
      다른 목록을 봐서, cook 목표는 뜨기만 하고 체크될 길이 없었다 —
      두 쪽이 같은 목록을 보게 했다
- [x] 만수에게 요리를 가져다주는 마무리가 그 목표를 확실히 닫는다
- [x] 예전 세이브는 불러올 때 고친다 (`kitchen_quest == "done"`이면 닫는다)
- [x] 검사 251 `COOKGUIDE_OK` · 벌목 문구 검사의 주사위 제거(조합법 드랍)

## M89. 집 꾸미기 — 가리키고 누른다 (완료)

- [x] **마우스로 꾸민다** — 가리키면 테두리+이름, 왼쪽 클릭으로 집기·놓기,
      오른쪽 클릭으로 제자리, 카탈로그도 눌러서 산다
- [x] 배경·캔버스의 `mouse_filter`가 STOP이라 클릭이 통째로 먹히던 것
      (그래서 마우스로는 아무것도 할 수 없었다)
- [x] 들고 있는 가구가 **가리킨 자리에 서 있도록** 따라온다
- [x] 안내에 **집는 키**를 적었다 — 예전에는 「집기·놓기」라고만 적혀 있어
      러그가 못 옮기는 물건처럼 보였다
- [x] 검사 252 `DECO_OK` · 문서 `docs/home_deco.md`

## M90. 큰 배너는 스토리 시작·완결에만 (완료)

- [x] 「교진 마을 도착 — 여기서부터가 진짜 하루다」 배너 삭제.
      마을에 닿았다는 것은 뒤이어 오는 우체부의 대사가 말해 준다
- [x] `tools/check_banners.py` — `story_banner`의 머리말이
      「메인 스토리 N 시작/완결」이 아니면 실패한다 (어중간한 배너 재발 방지)

## M91. 마을 리뉴얼 — 울타리·바닥·부지·언덕 (완료)

- [x] **울타리를 도트로 다시 그린다** (`ref/make_fence.js`). 참고 그림에서
      오려 낸 사진이라 혼자 매끈했고, 한 칸짜리 조각을 반복해 찍는 통에
      모퉁이에서 장이 허공으로 뻗었다. 이웃 넷의 꼴마다 한 벌씩 열여섯 장
- [x] **바닥에 결을 넣는다** — 마당 흙에 다져진 자리·발자국·갈라진 금·
      박힌 잔돌·지푸라기, 잔디에 홑잎. 결을 세 겹으로 겹쳐 네모 얼룩을 없앴다
- [x] **부지 아홉 곳**을 저마다 알아보게 채운다 (`main.PLOT_DECOR`).
      집 그림이 여덟 칸을 덮으므로 그 뒤에는 아무것도 두지 않는다
- [x] **광장** — 자갈을 깔되 가장자리는 들쭉날쭉하게, 가로등·평상·화분
- [x] **길은 줄기 폭 그대로 굽이친다** (`world_gen._paint_village_lane`)
- [x] **언덕 위의 집** — 도서관·대장간이 제 언덕에 얹히고 문 앞에 돌계단
      (`main.HILL_PLOTS`). 남쪽에 큰길이 있는 줄만 올릴 수 있다
- [x] `KYOJIN_ALBUM=1` — 검증은 걷고 부지 사진만 찍는 모드 (십수 초)

### 살아 있는 마을 (앰비언트) — renderer._update_ambient / _draw_clouds / _draw_glows

- [x] **구름 그림자** — 부드러운 큰 얼룩이 바람에 실려 맵 위를 흐른다.
      칸(560x430)마다 해시로 구름이 살거나 안 살고, 남보라 그늘로 기운다
- [x] **낙엽** — 화면 안 나무에서 이따금 잎이 한두 장 진다 (가을엔 주황,
      겨울엔 안 진다)
- [x] **발걸음** — 잔디에선 풀잎, 흙·자갈·모래에선 먼지가 걸음마다 인다
- [x] **밤 등불 빛무리** — 어둠(CanvasModulate)은 같은 캔버스를 통째로
      누르므로, 빛무리는 **딴 CanvasLayer**(follow_viewport)에 그린다.
      가로등·화로 자리를 몇 초에 한 번 모아 두고, 밤이 깊을수록 짙어진다
- 검증(KYOJIN_SHOT)에서는 앰비언트를 끈다 — 무작위 픽셀이 어서션을
  흔들면 안 된다. 앨범 맨 끝에 **밤 광장** 한 장을 추가로 찍는다
- [x] **움직임은 멀미와 싸운다** — 나무 흔들림을 크게 줬더니 숲에서는
      화면 대부분이 우듬지라, 걸을 때 **화면 전체가 울렁였다.** 눈 둘 곳
      (주인공)은 곧게 가는데 배경 전부가 물결치면 뇌가 「내가 흔들린다」로
      읽는다. 흔들림은 꼭대기 한 뼘만(UV.y*1.8), 한 파장으로(0.55Hz),
      1px 안쪽 진폭으로 — **멈춰 서면 보이고 걸을 땐 안 걸리는 정도**가 상한
- [x] **접지 그림자는 타원 두 겹** — 사람·NPC·짐승의 발밑 그림자를 딱딱한
      검정 네모에서 남보라 타원 두 겹(가장자리 옅게)으로. `KyojinMain.
      draw_ground_shadow`. 펫도 그림자를 얻었다 (여태 붕 떠 있었다)
- [x] **나무·바위의 접지 그림자** — 자연물은 여태 그림자 없이 떠 있었다.
      노드에 자식을 끼우면 온 코드의 「자식 0번 = 스프라이트」가 흔들리므로,
      세계보다 먼저 붙는 형제 레이어(main.shadows)에 renderer.
      _draw_object_shadows 가 타원을 그린다. **중심은 밑변보다 아래** —
      내려다보는 화면에서는 캐노피가 제 그림자의 위쪽을 다 가려서, 아래로
      고여야 눈에 보인다 (알파 0.52·캐노피 폭 95%로 키워도 안 보이던 이유)
- [x] **들판은 조용해야 한다** — 잔디 타일의 포기 5→2, 홑잎 9→4(tip 확률
      0.3), 잔돌은 세 장 중 한 장에 대각 두 점(바탕 62% 섞기), 꽃도 바탕에
      55% 섞어 눕혔다. **변형마다 붙는 장식은 들판에 수백 번 반복된다** —
      순색 점 하나가 물방울무늬 벽지를 만든다. 분홍 점의 범인은 흙이 아니라
      모래빛 잔돌이었다
- [x] **비는 바닥에 닿아야 비다** — 빗줄만 있으면 화면 앞 유리에 내리는
      것 같다. 닿는 자리에 잠깐 퍼지는 잔물결 고리(_draw_rain_splashes)를
      상태 없이 시간·해시로만 돌린다. 비 34개·폭풍 58개
- [x] **축사도 마을의 손으로** — 홀로 옛 그림체(정면 입면도 + 잡음·흐림)로
      남아 있던 마지막 건물. make_buildings.js 의 `barn` 항목으로 대체:
      barnred 널벽(칠한 널 plankk — 막돌 패스를 건너뛴다) · 크림 트림 ·
      X자 두짝문(barnDoors) · 다락문 · 잿빛 모임지붕. 납작했던 판형도
      다른 집과 같은 판(512x552)으로 — **몸집이 다른 것 자체가 이질감의
      절반**이었다. make_barn.js 는 은퇴(스텁만 남김), old_barn 오프셋은
      밑변 기준으로 다시 쟀다
- [x] **동굴도 같은 그림의 세계다** — 민무늬 보라 판에 바깥 바위를 격자로
      찍은 디버그 방이었다. 남보라 돌바닥 석 장 + 보로노이 막돌 벽 석 장
      (cave_floor/cave_wall, 씨앗 격자 4로 16칸에 감겨 이음매가 없다),
      벽이 바닥에 드리운 그늘, 세계나무 동굴은 같은 그림에 초록빛만.
      **보이는 칸만** 그린다 — 판이 100x58이라 다 그리면 육천 장이다
- [x] **가게 방도 집 안과 같은 결** — 체커보드 바닥과 한 색 벽을
      집 안(interior_ui)과 같은 문법으로: 가로 널 벽(단마다 톤·골·옹이),
      널판 바닥(널마다 톤·못·이음매), 창턱과 틀 윗변 빛, 벽 그늘 두 겹.
      방마다 다른 색(d.wall/d.floor)은 그대로 — 결만 통일한다
- [x] **소형 구조물도 같은 자로** (make_structures.js) — 동굴 입구·온천·
      해변 노점·옛 전망대가 옛 1px 밀도로 남아 「잘게 그린 스티커」였다.
      도트 한 칸 = 화면 2px 로 다시 그림: 막돌 언덕에 뚫린 아치(안은
      위가 어둡다), 김 오르는 바위 탕(김은 **윤곽선 뒤에** — 테가 붙으면
      지렁이가 된다), 줄무늬 차양 + 세로 널 계산대(가로 띠는 서랍장이
      된다), 바랜 나무 망루. 온천·전망대는 배율이 빠져 0.5배로 그려지고
      있었다 — OBJECT_SCALES 2.0. 셋 다 FADE_KINDS, 노점·전망대는 접지
      그림자
- [x] **깔개와 탁자는 색종이가 아니다** — 민무늬 사각형 두 겹이던 집·가게
      깔개에 테두리 띠·직조 결·네 귀 매듭을, 탁자에 상판 빛과 접지
      그림자를 넣었다

### 남은 일 (다음에)

- [x] **화로를 코드로 그린다** (`ref/make_props.js`) — 쌓은 돌 아궁이에 쇠 후드,
      아가리 안에 숯불. 불은 네 장을 돌려 흔들린다 (`LANDMARK_FRAMES`)
- [x] **아이템 아이콘을 살림 그림으로 바꾼다** — 모루·숫돌·장작더미·여물통·
      볏단·그물 틀·통·화단·손수레·책 무더기·표본 선반 열두 가지를 새로 그렸다.
      마당에서 광석·못·천 따위 가방 아이콘을 걷어냈다
- [x] **마당과 잔디가 잇는 자리** — 마당 흙이 울타리 **밖으로 새어 나간다**
      (`world_gen._lay_yard`). 경계 그림(`trod`)은 진작 있었는데 그게 하필
      말뚝이 선 줄이라 울타리 그림에 가려 아무도 못 봤다
- [x] **지붕을 위에서 본다** — 뾰족한 고깔을 넓은 사다리꼴(모임지붕)로 눕히고,
      용마루 뒤 윗면을 **제일 밝게** 뒤집었다 (`ref/make_buildings.js` 의
      `ridgeHalf`·`BACK_LO`/`FRONT_HI`). 참고 그림처럼 지붕이 「면」으로 읽힌다
- [x] **굴뚝은 지붕과 한 재료** — 색을 벽이 아니라 지붕 사다리(rp)에서 뽑고,
      켜도 기와와 같은 자(TW x TH)로 쌓는다. 갓 위로 윗면이 물러나고
      **아가리가 뚫려** 보인다. 굴뚝은 불 때는 집(대장간·여관·물가 오두막)에만
- [x] **화로의 상판을 넓힌다** — 굴뚝을 상판 **뒤쪽**에서 세워 앞 절반이
      남게 했다. 그을음은 눕는 면이 아니라 선 면에만 앉는다
- [x] **화로·모루를 마당에 붙인다** — 돌 사다리(ST)를 대장간 벽돌
      (WALL_PAL.stone)에 못 박아 다시 잡고, 밑동에 마당 흙빛(EARTH)이 튄
      자국을 얹었다. 모루는 **뿔을 길게** 다시 그렸다 (좌우 비대칭이 실루엣의 전부)
- [x] **화로를 44x64 로 키우고, 모루를 더 눕혔다** — 화로는 마당의 주인공인데
      곁의 작업대와 덩치가 비슷했다. 모루는 면을 다섯 줄로 눕히고 정면을
      두 줄로 깎았으며, 그루터기의 자른 면을 **타원**으로 다시 그렸다
- [x] **윗면을 더 확실히** — 상판은 열네 줄, 갓은 열 줄, 모루 면은 일곱 줄.
      상판이 받침보다 **두 칸 내밀고** 그 밑에 그늘을 깔았고, 넓어진 윗면에
      돌 줄눈을 깔았다 (`stoneTop`). `topFace` 의 좁아지는 폭은 줄 수가
      아니라 **물건 폭의 20%** 로 바꿨다 — 깊은 면일수록 치마가 됐다
- [x] **한 번 더 눕히고 키운다** — 화로 48x70 (상판 스무 줄 · 갓 열두 줄),
      모루 38x40 (면 아홉 줄 · 정면 두 줄). 폭 48은 부지 x=7 에서 집 그림에
      안 먹히는 한계다. 굴뚝은 반폭 7~9 로 **좁혔다** — 상판 뒤끝과 폭이
      비슷하면 둘이 붙어 그림이 통째로 피라미드가 된다
- [x] **chunky 모드 — 손으로 찍은 느낌** (일단 대장간만). 인위적으로 보이는
      건 색이 아니라 **잡음의 크기**였다: 조각을 키우고(기와 8x4 ·
      돌 8~10x5), 톤은 뭉치 단위로만 흔들고, 기와 밑변 귀퉁이를 둥글렸다.
      `spec.chunky` 로 켠다 — 마음에 들면 아홉 채 전부로 넓힌다
- [x] **땅에 얼룩을 만든다** — 바닥 장(variant) 세 장의 바탕 톤을 갈라 두고
      (`make_ground.js` 의 SHIFT), 고르는 쪽을 백색 잡음에서 **여러 칸짜리
      낮은 주파수 잡음**(`main._patch01` · `_patch_variant`)으로 바꿨다.
      잔디 7칸 · 마당 5칸 · 자갈 9칸 얼룩. 나무도 그루마다 옅게 물든다
- [ ] **남쪽 능선** (`SEA_OK 능선=false`)
- [x] 남은 이야깃거리는 **서브 퀘스트**로 둔다 — 튜토리얼은 부두의 첫 한 마리에서
      끝나고(`story._close_tutorial`), 그 뒤의 장은 전부 곁이야기로 내려간다
      (`game_data._resort_quests` · `tutorial_closed`)

## M8. 출시 준비

- [ ] Windows/macOS/Linux 익스포트 빌드
- [ ] Steamworks 연동 (도전과제, 클라우드 세이브) — GodotSteam 애드온
- [ ] 스팀 상점 페이지 (캡슐 이미지, 트레일러, 데모)
- [ ] 밸런스 테스트 및 폴리싱

---

### 개발 메모

- 캐릭터 아트는 `game/assets/ref/`의 원본에서 뽑는다. 남자는
  `ref/new_boy3/make_sprites.js`를 돌리면 시트 한 장에서 30장(걷기 15 ·
  서기 3 · 휘두르기 12)이 그대로 다시 생성된다 (128x192, 화면 키 184px).
  1·2세대(`new_boy/`, `new_boy2/`)는 안 쓴다 — **돌리면 같은 이름으로
  덮어써서** 걷기만 옛 그림으로 바뀐다.
- 검증: `godot --headless --path game --quit-after 120` 으로 무에러 실행 확인.
  `KYOJIN_SHOT=경로` 환경변수로 실행하면 스크린샷 자동 캡처 후 종료 (CI용).
- **그림 규칙 — 윗면을 그린다.** 화면은 정면이 아니라 비스듬히 위에서
  내려다보는 각이다. 세워 두는 물건은 예외 없이 두 면을 그린다:
  윗면 두 줄(제일 밝다 · 뒤 모서리는 한 칸 안으로) → **앞 모서리 한 줄
  (진하게)** → 정면 → 밑변 턱. 앞 모서리를 안 그으면 밝은 띠 하나로
  뭉개져서, 윗면을 그려 놓고도 「위에서 본다」가 안 읽힌다.
  (`ref/make_props.js` 의 `topFace`. 울타리도 같은 규칙)
- **윗면이 제일 밝다 — 지붕도 예외가 아니다.** 지붕 톤을 오래 「멀수록
  어둡다」로 두었더니 뒤로 누운 면이 지붕 위에 드리운 커다란 그늘로 보여서,
  아무리 각을 올려도 정면 입면도에 어두운 배경을 깐 꼴이었다. 하늘을 제일
  넓게 받는 면은 눕는 면이다. 그리고 **용마루를 길게 눕혀야** 그 띠가
  뒤로 뻗어 윗면이 된다 — 한 점으로 모이는 지붕은 위에서 봐도 삼각형이다.
- **색줄기는 건물 팔레트에서 가져온다** — `ref/make_buildings.js` 의 PAL 값을
  축으로 삼고 사이만 메워 사다리를 늘린다. 눈대중으로 「비슷한 색」을 따로
  잡으면 하나씩은 멀쩡한데 같이 놓았을 때 두 벌의 그림이 된다.
- **어울리는지는 눈이 아니라 밝기로 잰다.** 화로를 「집과 같은 회색 돌」로
  칠해 놓고도 마당에서 계속 떠 보였는데, 화면에서 재 보니 화로 126 ·
  마당 흙 127 로 **밝기가 똑같았다**. 색만 다르고 밝기가 같으면 물건이
  바닥에서 안 떨어진다. 곁의 건물(101)과 바닥(127) 사이에 놓이게 줄눈을
  한 단 더 파서 110 언저리로 내렸다. 크롭해서 평균값을 찍어 보는 게
  「따로 논다」를 고치는 제일 빠른 길이다.
- **폭이 같은 두 면은 톤만으로 안 갈린다.** 윗면과 정면이 같은 폭이면,
  아무리 밝기를 벌리고 접힘선을 그어도 한 장의 벽으로 붙어 버린다.
  윗면이 **정면보다 내밀고** 그 밑에 그늘 한 줄이 깔려야 — 부엌 조리대의
  앞턱과 같은 이치로 — 얹혀 있는 게 보인다.
- **넓은 윗면에는 재질이 있어야 한다.** 열네 줄짜리 상판을 민 색으로
  깔았더니 돌탑 위에 종잇장이 얹혔다. 정면이 돌인데 윗면만 민 색이면
  두 면이 다른 물건이 된다. 줄눈은 **뒤로 갈수록 촘촘하게** — 줄 간격이
  곧 기울기다 (기와 켜와 같은 규칙).
- **집은 낮고 넓적해야 마을에 앉는다.** 층고 22+22·지붕 40의 집은 폭 90에
  키 120이 넘는 탑이라, 위에서 내려다보는 맵에서 혼자 일어서 있었다.
  1층 20 + 다락 14 + 지붕 34 (DEPTH 30) — 키가 폭과 엇비슷해야 참고
  맵처럼 「마을에 놓인 집」이 된다. 다락 창·문·간판은 줄어든 층고에 맞춰
  같이 내려앉는다.
- **입체감은 접지 그림자와 경계 단이 만든다.** 물건들이 「따로 노는」 건
  그림체가 달라서만이 아니라 서로 관계가 없어서다: 건물 밑에 반투명
  접지 그림자(groundShadow)가 깔려야 집이 땅을 누르고, 길 가장자리에
  연석 한 단(path_curb_*)이 있어야 길이 흙에 번진 얼룩이 아니라 「깔린
  것」이 된다. 참고 맵의 입체감은 무늬가 아니라 이 관계들이다.
- **벼랑도 막돌이다.** 세로 결로 그린 절벽 면은 마을이 전부 막돌로 간
  뒤에 혼자 잿빛 널판 담이었다. 같은 보로노이 규칙(rockFace)로 갈면
  언덕이 「돌을 쌓은 축대」가 되어 벽·화로·길과 같은 돌 언어가 된다.
- **막돌은 보로노이로 쌓는다.** 격자 벽돌로는 손으로 쌓은 돌이 안 된다 —
  씨앗점을 흩뿌리고 픽셀마다 가장 가까운 씨앗을 찾으면 돌 하나하나가
  크기·모양 제각각인 다각형 덩이가 된다 (make_props.rubble). 줄눈은 첫째·
  둘째 씨앗의 거리가 비슷한 골, 돌마다 한 색, 씨앗 격자는 구조의 오분의 일.
- **다 그리지 않는다.** 돌·기와를 한 장도 빠짐없이 격자로 채우면, 어떤
  색을 입혀도 「기계가 채운 무늬」다. 손으로 찍은 그림은 면을 평평하게
  비워 두고 **드문 획과 돌 몇 덩이**로만 재질을 암시한다 — 기와는 서너
  장에 획 하나(밑변에 2~5칸), 벽은 회벽에 박힌 돌 대여섯 덩이
  (sparseStones), 길은 조용한 자갈밭에 도드라진 돌 두어 개. 바탕이
  조용해야 놓은 것이 보인다.
- **장의 밝기를 흔들지 말고 섞임새를 흔든다.** 바닥 장(variant)마다
  밝기를 +-1 통째로 올리내리면 이웃 타일과의 경계가 세로줄로 드러난다.
  같은 두 톤을 쓰되 어두운 얼룩의 **비율**만 장마다 다르게 하면
  경계가 안 보인다 (cobble 의 MIXSHIFT).
- **선이 돌을 만들고, 평평함이 조용함을 만든다.** 줄눈을 아래·오른쪽에만
  긋는 벽돌 쌓기로는 몇 번을 고쳐도 벽돌담이었다. 손으로 찍은 돌은 알
  하나하나가 **사방으로 어두운 줄눈에 둘러싸인 둥근 덩어리**고, 속은
  잡음 없이 평평하며, 윗줄에만 빛이 한 줄 얹힌다. 세로 줄눈은 알의
  왼끝 **한 칸이면 된다** — 오른끝까지 그으면 이웃 알과 겹쳐 그물이 된다.
  (길 cobble · 집 boulderCourse · 화로 towerRow 가 전부 이 한 규칙)
- **사람 손은 균일한 난수를 못 찍는다.** 그림이 「인위적」으로 보일 때
  의심할 것은 색보다 잡음의 주파수다. 기계 그림은 조각마다 톤을 흔들어
  온 면이 고르게 자글거리고, 손 그림은 조각이 크고 면이 조용한 대신
  얼룩이 서너 장씩 뭉쳐 다닌다. 조각을 키우고 난수를 뭉치(2x2) 단위로
  옮기는 것만으로 같은 팔레트가 손맛이 난다 (`make_buildings.js` CHUNKY).
- **땅이 밋밋한 건 장이 못나서가 아니라 얼룩이 없어서다.** 바닥 세 장을
  칸마다 백색 잡음으로 골랐더니, 가까이선 어지럽고 멀리선 통짜 한 색이었다
  — 세 장이 잘게 섞이면 결국 평균이다. 참고 그림의 땅이 살아 보이는 건
  밝은 자리와 그늘진 자리가 **대여섯 칸에 걸쳐** 번갈아 나오기 때문이다.
  장마다 바탕 톤을 갈라 두고, 격자점 잡음을 smoothstep 으로 이어(_patch01)
  낮은 주파수로 고르면 그 얼룩이 나온다. 잔 잡음을 20% 섞어 등고선을 허문다.
  단, 자갈처럼 사다리 한 단이 큰 재료는 통으로 밀면 딴 포장이 된다 —
  알마다 반쯤만 먹인다.
- **좁아지는 폭은 줄 수가 아니라 물건 폭이 정한다.** 줄마다 한 칸씩
  좁혔더니 깊은 면일수록 뒤가 반토막이 나서 치마가 됐다. 뒤로 가서
  좁아지는 건 원근이지 원뿔이 아니다 — 맨 뒤에서 폭의 20%면 충분하다.
- **원기둥의 자른 면은 타원이다.** 그루터기 윗면을 가로 직선으로 긋고 그
  위에 몇 줄을 얹는 식으로는, 각도를 아무리 높여도 네모난 궤짝으로 읽힌다.
  타원의 **앞쪽 호가 곧 윗면과 옆면을 가르는 선**이다. 다만 타원을 너무
  크게 잡으면 이번엔 팬케이크가 된다 — 자른 면과 껍질 옆면이 둘 다 보여야
  원기둥이다.
- **물건을 땅에 붙이는 건 그림자가 아니라 밑동의 흙탕물이다.** 비가 마당
  흙을 튀겨 아래 한 뼘을 늘 더럽힌다. 그 흙빛을 바닥 그림(EARTH)에서
  그대로 가져와 발치에 얹으면, 갓 만든 물건이 「거기 서 있던 것」이 된다.
  잔 확률로 뿌리면 후추가 되므로 큰 칸(2x2)으로 자리를 먼저 정한다.
- **한 칸에 담기려면 윤곽만으로 이름이 나와야 한다.** 숫돌은 벽시계가 되고
  통은 쓰레기통이 됐다. 그리고 물건끼리도 실루엣이 달라야 한다 — 연장
  걸이를 무기 거치대와 같은 「기둥 둘에 가로대」로 그렸더니 둘이 구분되지
  않아, 연장 쪽을 작업대(엎어 놓은 판)로 바꿨다.
- 부지 그림만 빨리 보고 싶으면 `KYOJIN_ALBUM=1 KYOJIN_SHOT=경로/` — 검증
  시퀀스는 한 줄도 돌지 않고 부지를 한 곳씩 돌며 사진만 찍는다 (십수 초).
  본 시퀀스에 사진 자리를 끼워 넣으면 걸음 수가 밀려 뒤의 검사가 어긋난다.
